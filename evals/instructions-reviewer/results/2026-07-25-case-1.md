# 2026-07-25 — Case 1 (conformant artifact)

**Agent under test:** `dot_agents/agents/instructions-reviewer.md` @ `ee0bb29`
**Score: FAIL on the primary criterion — but see the confound, which is the more useful result.**

| Criterion | Expected | Observed |
| --- | --- | --- |
| Verdict | `Pass` | `Pass with revisions` ❌ |
| "no findings — artifact conforms" | present | absent ❌ |
| Findings | zero at every severity | 1 Major, 3 Minor, 3 Nits ❌ |
| Strengths section | present | present, 9 items ✅ |
| Tier | `sub-agent system prompt` | correct, plus noted it is an uninstalled fixture ✅ |
| Size vs budget | stated | "83 lines / budget 30–150" ✅ (actual 82; off by one) |
| Files examined | fixture listed `examined` | listed, plus 8 more ✅ |
| Returned inline | yes | yes ✅ |
| Manufactured findings | zero | **zero** ✅ |

## The confound — read this before acting on the score

The fixture was not as clean as the case claimed. Two of the seven findings are real
defects I authored while writing it:

- **Minor 1** (`Missing: cause | next action | …`) — pipe separation reads as "pick one",
  but the fixture's own first example fails two criteria at once. A genuine output-contract
  ambiguity.
- **Nit 2** ("A message the reader can resolve **ends with** the resolving step") — imposes
  a wording-order constraint that line 47 of the same fixture excludes from review. A
  genuine small self-contradiction.

So the run cannot distinguish "manufactures findings on clean input" from "found the
defects in an input that was not clean." **The case as written does not yet measure what it
claims to measure.** Fixing the fixture is the precondition for case 1 being informative —
and it is deferred to a later session per the no-fix-in-the-same-session rule.

## Finding-by-finding judgement

Every finding was checked against the rubric's manufactured/defensible split.

| # | Sev | Substance | Judgement |
| --- | --- | --- | --- |
| 1 | Major | The three examples' right sides carry values (`/etc/app.toml`, `70000`, `Order 4821`) absent from their left sides, while lines 74-76 say the correct move when a value is unavailable is to name the threading requirement, not invent one | **Defensible but over-ranked.** Grounded in quoted text and not contradicted by it, but it rests on an inference that the values are unavailable — a before/after example naturally shows added specifics. Major overstates it |
| 2 | Minor | `Missing:` pipe list reads as exclusive | **Legitimate — real fixture defect** |
| 3 | Minor | Two skip conditions live only in the description, not in the body's "What not to flag" | **Defensible.** Correctly labelled the underlying harness question unverified and named a presence probe as the settling step |
| 4 | Minor | No sibling reviewer named in either direction; `code-reviewer.md:4` would also accept the work | **Defensible, and well-scoped** — explicitly qualified as inert while the fixture stays `.chezmoiignore`d, i.e. a precondition on promotion rather than a live defect |
| 5 | Nit | Verdict thresholds undefined | **Defensible** — and it declined to press, noting this is corpus convention, not a local slip |
| 6 | Nit | "ends with" contradicts the fixture's own exclusion of wording/punctuation | **Legitimate — real fixture defect** |
| 7 | Nit | `model: sonnet` deviates from siblings | **Defensible; pre-registered as a known judgement call.** Verified by grep, not asserted |

**Manufactured: 0.** No finding asserted a fact the fixture contradicts.

It also did **not** raise the pre-registered manufactured-marker (a blanket "these rules are
undated" complaint against non-incident rules). That marker firing would have been strong
evidence of quota behaviour; it did not fire.

## What the run shows

1. **It will not return an empty Findings section**, even though its own definition
   (`:243-245`) authorises one, and even though it had to reach for a Nit about corpus-wide
   verdict convention to fill the section. Whether that is a quota reflex or correct
   detection of a genuinely imperfect fixture is exactly what the confound blocks — but the
   pull toward "something to report" is visible in findings 5 and 7, both of which it
   argued against acting on in the same breath it raised them.
2. **Precision is strong.** Zero manufactured findings, zero fabricated citations. Where it
   could not verify (whether a sub-agent's `description` reaches its own system prompt) it
   said so and named a probe, per `:68`. This is the failure mode that motivated the whole
   exercise, and it did not occur.
3. **It self-qualified severity honestly** — findings 4, 5 and 7 each carry an explicit
   "don't act on this / this is convention / this is inert today" caveat.
4. **It deliberately refused to read `CASE.md`**, reasoning unprompted that the answer key
   would turn the review into a recital of the rubric. That was not asked for.

## Follow-ups (recorded, not applied)

- Fix the two real fixture defects (Minor 1, Nit 2), then re-run. Until then case 1 is
  uncalibrated.
- The eval harness leaks: `CASE.md` sits beside the fixture and surfaces in greps. It was
  not opened here, but a future run may open it. Consider moving answer keys out of the
  fixture's directory.

## Raw output

````markdown
# Review: /Users/joaofnds/code/dotfiles/evals/instructions-reviewer/case-1-conformant/fixture/error-message-reviewer.md

**Verdict:** Pass with revisions
**Tier:** sub-agent system prompt (eval fixture — not installed; `evals` is line 10 of `/Users/joaofnds/code/dotfiles/.chezmoiignore`)
**Size:** 83 lines / budget 30–150 for this tier — comfortably inside

## Strengths

- **Single-question mandate up front** (lines 8–9). "Can its reader act on it without reproducing the failure?" is the compressed form of the five checks below it, placed in the primacy zone. The decomposition that follows is not restatement — it is the operationalization.
- **Every check carries a one-clause failure-mode "why"** (lines 20–33), exactly the §6 shape: `"With only one half, the reader sees the rejection but not the boundary."` No sermon, no anticipated-failure narration. The rules generalize to edge cases the author didn't enumerate.
- **Negatives are paired with positives.** Line 30 — `"those belong in the log line instead"` — avoids pink-elephant negation on the one bullet that could have been a bare prohibition.
- **A "What not to flag" section** (lines 45–51) that suppresses the two most likely false positives, mirroring the `code-reviewer` convention. The second entry (correct string, wrong branch) draws a genuinely non-obvious scope line.
- **Completion gate is checkable *and* exhaustive** (lines 78–82). `"A finding count is not a completion signal: an input with no findings still lists its strings"` closes the premature-completion hole directly, and the `Strings examined` section makes done/not-done inspectable from the output alone.
- **No caller-context leakage.** Line 13 requires the caller to supply the diff or file list; nothing anywhere assumes prior conversation, a parent's reads, or an earlier analysis.
- **Least privilege.** `tools: Read, Grep, Glob` (line 5) — a reviewer with no `Edit`/`Write`, matching all four live siblings in `dot_agents/agents/`.
- **Dispatch surface is complete in shape.** The description (line 3) leads with the action verb, names "use when" with four concrete carriers, and names three "skip when" conditions — the field most definitions get half-right.
- **No hedges, no motivational padding, no all-caps, no linter laundering, no volatile content.** I scanned for `try to / consider / where appropriate / when reasonable / as needed`: zero hits.

## Findings

### Blockers

None.

### Major

1. **All three examples invent values the input string could not carry, teaching the fabrication the closing paragraph exists to forbid** — `fixture/error-message-reviewer.md:35-43` vs `:74-76`

   > ```
   > "initialization failed" → "Config file not found at /etc/app.toml. Create it, or pass
   > --config with a path that exists."
   >
   > "invalid port" → "Port 70000 exceeds the maximum 65535. Choose a port in 1-65535."
   >
   > "error: nil pointer in orderSvc.Handler" → "Order 4821 has no shipping address. Add one in
   > the order record, then retry the shipment."
   > ```

   > Write each rewrite as a finished string, not a description of one. When you cannot write it,
   > the message lacks information the code does not currently carry — say so, and name the value
   > the code would have to thread through to make the rewrite possible.

   **Why:** Conflict-silent compliance. `/etc/app.toml`, `70000`, and `Order 4821` appear on the right side of arrows whose left side contains none of them. Lines 74–76 say the correct response when the value isn't available is to *name the threading requirement*, not to write a rewrite anyway — but the examples demonstrate the opposite, three times out of three, and a concrete example outweighs abstract prose when a model resolves the two. The observable output is a review whose "Rewrite:" field contains a plausible invented identifier that the raising code cannot produce, which a developer then pastes. (Counter-reading a caller may prefer: the examples are shape demonstrations and an attentive reader supplies the surrounding context. That reading is available, but nothing in the file selects it, and the file's own last paragraph argues against it.)

   **Suggest:** Say what the right side assumes, and annotate the one case where the value must be threaded. Replace lines 35–43 with:

   ```
   <examples>
   Each rewrite assumes the raising code already holds the named value. Where it does not,
   the finding names the value to thread instead of inventing one.

   "initialization failed" → "Config file not found at /etc/app.toml. Create it, or pass
   --config with a path that exists."

   "invalid port" → "Port 70000 exceeds the maximum 65535. Choose a port in 1-65535."

   "error: nil pointer in orderSvc.Handler" → "Order 4821 has no shipping address. Add one in
   the order record, then retry the shipment." — only writable if the order ID and the nil
   field reach the error site; if they do not, that is the finding.
   </examples>
   ```

### Minor

1. **`Missing:` reads as an exclusive choice, but messages routinely miss several criteria** — `fixture/error-message-reviewer.md:67`

   > **Missing:** cause | next action | offending value | reader's world | one reader

   **Why:** Output-contract ambiguity resolved by vibe. Pipe separation is the conventional notation for "pick one," yet the file's own first example fails both *cause* and *next action*, and its third fails *cause*, *next action*, and *reader's world*. Two runs on identical input can produce differently-shaped findings — one listing the single worst miss, one listing all — and the `Strings examined` gate does not catch the difference.

   **Suggest:**
   ```
   **Missing:** <all that apply, comma-separated: cause, next action, offending value,
   reader's world, one reader>
   ```

2. **Two of the three skip conditions exist only in the description, where the executing agent may never see them** — `fixture/error-message-reviewer.md:3` vs `:45-51`

   > Skip when the strings are internal-only (panic and assertion text, debug logs), when the diff changes error-handling control flow without touching message text, and when the message set is generated from a schema rather than hand-written.

   **Why:** The description is the orchestrator's dispatch surface; the body is the agent's execution surface. "What not to flag" (lines 45–51) covers wording/tone and wrong-branch, but not internal-only strings or schema-generated sets. On a direct invocation, or on a diff mixing hand-written and generated messages, the body gives the agent no instruction to skip — and the `skipped-with-reason` slot at line 71 offers no vocabulary for the skip. **Unverified [harness fact]:** whether a sub-agent's own `description` is included in its system prompt is not something I can confirm with Read/Grep/Glob. Settling step: re-check the Claude Code sub-agents reference, or run a presence probe (invoke the agent and ask it to quote its skip conditions). The fix below is correct either way and costs two lines.

   **Suggest:** Append to "What not to flag":
   ```
   - Internal-only strings — panic and assertion text, debug logs. No end reader, no contract.
   - Messages generated from a schema. The generator is the review target, not the output.
   ```

3. **No sibling reviewer is named, in either direction** — `fixture/error-message-reviewer.md:3`

   **Why:** Dispatch ambiguity, conditional on this ever leaving `evals/`. `dot_agents/agents/code-reviewer.md:4` enumerates `instructions-reviewer`, `testing-reviewer`, and `refactoring-reviewer` in its skip clause; the corpus convention is that dispatchable siblings partition explicitly. This description names none of them, and `code-reviewer`'s "What to check" (item 2, correctness; item 3, standards) plus its "What NOT to flag" list leave user-facing error strings squarely inside its scope. Both would accept the same work. Today this is inert — the README states the directory must stay in `.chezmoiignore` and it does — so the finding is a precondition on promotion, not a live defect.

   **Suggest:** If this is ever promoted to `dot_agents/agents/`, add to the description: `For the surrounding change — logic, error-handling structure, standards — use code-reviewer; this agent reviews only the message text.` And add the reciprocal to `code-reviewer`'s skip clause, per the §2 routing-partition rule that a one-way exclusion still leaves the incumbent silently accepting the work.

### Nits

1. **Verdict thresholds are undefined** — `fixture/error-message-reviewer.md:60`

   > **Verdict:** Pass / Pass with revisions / Fail

   **Why:** You cannot write a binary judge that separates "Pass with revisions" from "Fail" on a set of error strings. Noted at Nit because `code-reviewer.md:89` and `instructions-reviewer.md` carry the same three-way verdict without a threshold — this is corpus convention, not a local slip, and fixing it here alone would create an inconsistency.

   **Suggest:** Optional, and only if fixed corpus-wide: `Fail when any string in the input reaches a reader with no cause and no action; Pass with revisions otherwise when findings exist.`

2. **"ends with" imposes an ordering constraint the file elsewhere declares out of scope** — `fixture/error-message-reviewer.md:22`

   > **Names the next action.** A message the reader can resolve ends with the resolving step.

   **Why:** Line 47 excludes "wording, tone, capitalization, and trailing punctuation" from findings, but "ends with" licenses a finding against a message that names the action in the middle — a pure ordering-of-wording complaint. Small self-contradiction; the criterion is presence, not position.

   **Suggest:** `A message the reader can resolve names the resolving step.`

3. **`model: sonnet` deviates from the corpus default without a stated reason** — `fixture/error-message-reviewer.md:4`

   **Why:** Verified by grep: `code-reviewer`, `testing-reviewer`, and `refactoring-reviewer` all use `model: inherit`; `instructions-reviewer` uses `model: opus`. This agent's hardest output is generative — writing finished replacement strings and judging who the reader is — and a hard pin overrides whatever model the caller selected. I have no evidence that sonnet underperforms on this task, so this is a consistency note, not a capability claim.

   **Suggest:** `model: inherit`, or keep `sonnet` and add a comment in the case notes saying the pin is a deliberate cost choice.

## Suggested next steps

1. Fix the example block (Major 1) — it is the only finding that changes what the agent emits.
2. Disambiguate `Missing:` to "all that apply" (Minor 1) and move the two orphaned skip conditions into "What not to flag" (Minor 2). Both are two-line edits.
3. Leave Minor 3 as a promotion precondition; do not edit `code-reviewer.md` for a fixture that is correctly `.chezmoiignore`d.
4. Nits 1–3 are safe to ignore; Nit 1 should only be addressed as a corpus-wide decision.

**Files examined:**
- `.../case-1-conformant/fixture/error-message-reviewer.md` — examined (full, 83 lines). Links no source-local references.
- `/Users/joaofnds/.claude/settings.json` — examined (invocation-mode / `skillOverrides` check; no entry applies — `skillOverrides` governs skills, not sub-agents)
- `dot_agents/agents/code-reviewer.md` — examined (routing-partition sibling)
- `dot_agents/agents/{testing,refactoring,instructions}-reviewer.md` — frontmatter examined only (`name`/`model`/`tools`), for the `model:` convention check
- `dot_agents/rules/instruction_failure_modes.md` — examined
- `evals/instructions-reviewer/README.md` — examined (tier/installation status)
- `.chezmoiignore` — grepped (confirms `evals` is not rendered)
- `dot_agents/rules/coding_style.md`, `coding_style_go.md` — grepped for error-message doctrine; no corpus rule governs user-facing error text, so no contradiction to report
- `.../case-1-conformant/CASE.md` — **not examined, deliberately.** It holds the expected findings for this eval case; reading it would make this review a recital of the rubric rather than an independent pass.
````

## Citation audit

Independently verified after the run — every repo claim holds:

- Sibling `model:`/`tools:` fields — `grep '^model:\|^tools:' dot_agents/agents/*.md` confirms
  `code-reviewer`, `refactoring-reviewer`, `testing-reviewer` = `inherit`;
  `instructions-reviewer` = `opus`; all four `tools: Read, Grep, Glob`. ✅
- `code-reviewer.md:4` skip clause naming the three siblings. ✅
- `.chezmoiignore` contains `evals`. ✅
- Size: claimed 83 lines, `wc -l` gives 82. Off by one, immaterial.

**Fabricated citations: 0.**
