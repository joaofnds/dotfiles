# 2026-08-17 (re-run) — Case 1 (conformant artifact)

**Agent under test:** `dot_agents/agents/instructions-reviewer.md` at commit
`e5b76b47c947c290b39c1484a240bfbc08b062d1` (the `**Size:**` line-count requirement
deleted). Applied to `~/.agents/agents/instructions-reviewer.md` before the run.
**Answer key:** `case-1-conformant/CASE.md`, as corrected in `1911e4c`.
**Invocation:** the `instructions-reviewer` agent, prompt verbatim from CASE.md §Input,
one run, no added scaffolding.

Why a re-run: the key's scoring rule changed in `1911e4c` — the verdict string is no
longer the signal — so the earlier run on this date is not comparable.

## Score

**Pass.** CASE.md §Scoring: "**Pass** — no Blocker, no Major, and no manufactured
finding." The run returned zero Blockers, zero Majors, and one Minor that quoted fixture
text and claimed nothing the fixture contradicts.

Precision in the case's own terms: 1 defensible Minor, 0 manufactured. The verdict string
was `Pass with revisions`, which the reviewer's own mapping requires for any Minor and
which §Scoring explicitly does not score.

### Against the expected behaviour

| # | Expected | Observed |
| --- | --- | --- |
| 1 | `**Verdict:** Pass` and literal `No findings.` | Neither. `Pass with revisions`; "None." under Blocker and Major, one entry under Minor |
| 2 | Findings empty at all three severities | One Minor. No `Nit` heading — the ladder held |
| 3 | `**Tier:** sub-agent system prompt`, `**Size:**` against the 30–150 budget | Both present, budget named and cited |
| 4 | `## Files examined` lists the fixture as target and examined | Present, plus declared-read evidence entries the key permits — and one entry the key does not (see Confound) |
| 5 | Returned inline | Yes |

`## Apply state` omitted, as the key allows.

### The one Minor, judged

> **No-invention constraint sits in an example gloss, scoped to one of the two admitted
> inputs** — `<examples>` block — `values the surrounding diff already carries`

**Defensible, not manufactured.** Verified against the fixture: line 36 is the only
sentence forbidding invented values, it is example commentary rather than a rule, and
line 13 admits "a diff or a file list" — so on the file-list path the clause has no
referent. Both facts are in the target text.

This is a refinement of the very line `1911e4c`'s fixture repair added to close the
2026-07-25 confound. The key's fixture table calls that row clean ("values sourced from
the diff by their preamble"); the run accepts the preamble exists and objects to its
scope. Not covered by §Known judgement calls, and not contradicted by the fixture.

### Confound — a stray file in the rendered fixture directory

The run cited a sibling fixture, `error-message-drafter.md` §Drafting rules, as evidence
that the same constraint is stated as a rule elsewhere. That file exists **only** in the
rendered tree, at
`~/.agents/evals/instructions-reviewer/case-1-conformant/fixture/error-message-drafter.md`
(mtime 2026-08-10). It is unmanaged — `chezmoi managed` on that directory lists only
`error-message-reviewer.md`, and `git log --all` for the name is empty.

The citation is real, not fabricated: the reviewer read a file that is on disk. But
CASE.md §The fixture claims the fixture "links to no other file, so no transitive read can
drag real corpus findings into the result", and a stray sibling in the same directory
breaks that premise — the run used it for the routing-partition sweep as well as for the
Minor. The Minor stands without it, since its two load-bearing facts come from the target.

Not repaired here: this was a scoring session.

### Line count, again

The run reported `85 lines`; `wc -l` gives 84. Sixth consecutive one-high count. The
`**Size:**` template no longer asks for a count — this run volunteered one, in the old
template's shape ("85 lines / 30–150"). Cause unsettled between habit and the run loading
a definition cached before the apply. Probe: run this case first in a fresh session.

### Process deviation

The run appended an unlabelled block after `## Findings` ("Everything else swept
clean…", nine bullets recording what passed). §Output format specifies the document's
sections and this is not one of them. It is not a finding — each bullet records a clean
sweep — so it does not touch the score, and no `Nit` rung was introduced.

## Run output, verbatim

# Review: `/Users/joaofnds/code/dotfiles/dot_agents/evals/instructions-reviewer/case-1-conformant/fixture/error-message-reviewer.md`

**Verdict:** Pass with revisions
**Tier:** sub-agent system prompt
**Size:** 85 lines / 30–150 (sub-agent budget, `artifact-class-checks.md` §Per-file budgets)

## Findings

### Blocker

None.

### Major

None.

### Minor

1. **No-invention constraint sits in an example gloss, scoped to one of the two admitted inputs** — `dot_agents/evals/instructions-reviewer/case-1-conformant/fixture/error-message-reviewer.md` — `<examples>` block
   > values the surrounding diff already carries

   **Why:** Weak framing obscuring a condition. This is the only place the prompt forbids inventing values, and it appears as commentary on the examples rather than as a rule — while `## Input` admits `a diff or a file list`, so on the file-list path the clause has no referent. The `## Output` rule does not close the gap: `Write each rewrite as a finished string, not a description of one.` plus the "when you cannot write it" branch handles the honest-failure case only; a rewrite containing a fabricated identifier never enters that branch. Example 3 models exactly that shape (`"Order 4821 has no shipping address."`), so the consuming reviewer can emit a prescribed error string with a plausible invented value, which the caller then writes into code. The sibling fixture states the same constraint as a rule — `error-message-drafter.md` §Drafting rules: "Invent no value, constraint, action, or diagnosis."
   **Suggest:** Delete line 36 and fold the constraint into the rewrite rule, where it binds both input shapes:

   > Write each rewrite as a finished string using only values the input already carries, not a description of one.

   Changes behavior: the prohibition becomes a rule rather than example commentary, and it applies to a file-list input as well as a diff. Cost: net shorter — one gloss line removed, four words added; nothing is lost, since the gloss's only content was this constraint.

Everything else swept clean. Recorded because it constrains any follow-up edit:

- Frontmatter is well-formed, fields are the standard sub-agent set, no duplicate keys. `tools: Read, Grep, Glob` is least-privilege for a reviewer (no `Edit`/`Write`) and every entry survives the background-run narrowing (`instruction_external_facts.md` §Harness mechanics, last full pass CLI 2.1.222), so no run-mode declaration is owed.
- The description carries both "Use when" and "Skip when", trigger front-loaded. It is well under the recorded skill-listing cap; no sub-agent description cap is recorded, so no truncation claim is made either way.
- Capability closure holds: every mandated action (enumerate strings, cite `<absolute file path>:<line>`, read surrounding code for values) is reachable with Read/Grep/Glob, and the inline return matches the absence of a write tool.
- The completion gate is checkable and exhaustive, and it names the premature-completion trap directly (`A finding count is not a completion signal`). The five `Missing:` labels map 1:1 onto the five `## What to check` bullet titles — the partition covers exactly once.
- `## What to check` and `## What not to flag` do not overlap, and the wrong-branch exclusion complements the description's control-flow skip rather than duplicating it.
- Routing partition with the sibling fixture is two-sided: `error-message-drafter.md` skips "changeset conformance", and this file's input gate (`a diff or a file list`, else request) refuses the drafter's record-shaped input. No dispatch ambiguity.
- No cross-file mirror to mark: grepped `~/.agents` for error-message doctrine and no rule file governs user-facing error text.
- No dates, version notes, incident narration, hardcoded repo paths, external claims needing a store entry, all-caps rules, or vague hedges. Rationale is one sentence per bullet, per the bar's `Written for someone acting now`.

## Files examined

- `/Users/joaofnds/code/dotfiles/dot_agents/evals/instructions-reviewer/case-1-conformant/fixture/error-message-reviewer.md` — target — examined (whole file). Reviewed as the sub-agent definition it is in form; nothing loads it as a governing artifact — it is an eval fixture, rendered to `/Users/joaofnds/.agents/evals/...` and not into any agents directory.
- `/Users/joaofnds/.agents/rules/writing_instructions.md` — evidence (the bar) — examined
- `/Users/joaofnds/.agents/rules/instruction_failure_modes.md` — evidence — examined
- `/Users/joaofnds/.agents/rules/instruction_external_facts.md` — evidence (§Harness mechanics: background tool set, skill-listing cap, sub-agent `tools` semantics) — examined
- `/Users/joaofnds/.agents/agents/references/artifact-class-checks.md` — evidence (§Per-file budgets, §Sub-agent specifics) — examined
- `/Users/joaofnds/.agents/agents/references/dispatch-fields.md` — evidence — examined
- `/Users/joaofnds/.agents/evals/instructions-reviewer/case-1-conformant/fixture/error-message-drafter.md` — evidence (sibling: routing partition, no-invention rule form) — examined
- `/Users/joaofnds/.agents/agents/code-reviewer.md` — evidence (house frontmatter and sibling-naming convention) — sampled (frontmatter and first 40 lines)
- `/Users/joaofnds/.agents/agents/` — evidence — sampled (`name`/`model`/`tools` grep across the four live agent definitions)
