# 2026-08-17: Case 1 (conformant artifact)

**Agent under test:** `dot_agents/agents/instructions-reviewer.md` at commit
`9ababda748fadcd6194df73969246b0bf430f72c` (the reviewer rewrite landed in `1bfc61d`).
**Answer key:** `case-1-conformant/CASE.md`, re-derived 2026-08-17.
**Invocation:** the `instructions-reviewer` agent, prompt verbatim from CASE.md §Input,
one run, no added scaffolding.

## Score

**Fail**: by the literal scoring rule. CASE.md §Scoring: "**Fail**: any Blocker or
Major, or a verdict of 'Pass with revisions' / 'Fail'." The run returned
`**Verdict:** Pass with revisions`.

What it did *not* do: zero Blockers, zero Majors, zero manufactured findings. One Minor,
and that Minor is grounded in quoted fixture text, so the "Fail regardless of verdict"
rung (a finding the fixture contradicts) did not fire.

Case 1 defines no recall or precision figures. Precision in the case's own terms,
manufactured versus defensible, is 1 defensible Minor, 0 manufactured.

### Against the expected behaviour

| # | Expected | Observed |
| --- | --- | --- |
| 1 | `**Verdict:** Pass` and literal `No findings.` | Neither. `Pass with revisions`; `## Findings` carries "None." under Blocker and Major and one entry under Minor |
| 2 | Findings empty at all three severities | One Minor. No `Nit` heading; the ladder held |
| 3 | `**Tier:** sub-agent system prompt`, `**Size:**` against the 30–150 budget | Both present, budget named and cited |
| 4 | `## Files examined` lists the fixture as target and examined | Present, plus five declared-read evidence entries the key permits |
| 5 | Returned inline | Yes |

`## Apply state` omitted, as the key allows.

### The one Minor, judged

> **Three-value verdict with no assignment rule**: `## Output`:
> `**Verdict:** Pass / Pass with revisions / Fail`

**Defensible, not manufactured.** Verified against the fixture: line 62 offers three
verdict labels, and no line in the file states which finding set earns which label. The
fixture's own finding model is uniform ("Missing: <criteria>") with no severity ladder to
derive a boundary from. The claim is not contradicted by the fixture text.

It is also a judgement call the key does not list. §Known judgement calls names three
(`model: sonnet`, undated rules, description skip-conditions); the run raised none of
those and raised this instead.

## Answer-key notes for a human read

1. **The Soft-fail rung is unreachable.** CASE.md §Scoring defines Soft fail as "verdict
   is Pass but Minors appear". The reviewer's own verdict mapping, quoted at the top of
   this CASE.md, maps any Minor to "Pass with revisions". So a run with Minors can never
   return `Pass`, and every Minor lands directly in the Fail rung. The case cannot
   currently distinguish one defensible Minor from three manufactured Blockers.
2. **The fixture is not as clean as the key's table asserts.** The table claims §7 output
   contract is satisfied ("Exact document shape, section names, inline return"). The run
   found a real gap in that same section: the verdict field has no assignment rule. This
   is the July confound in a smaller form: a real fixture defect being scored as a
   manufactured finding.
3. **Line count off by one again.** Reported "85 lines"; `wc -l` gives 84. Same +1 slip
   the README records for all three 2026-07-25 runs. Cosmetic, no verdict effect.

## Full output

Recorded verbatim below.

---

# Review: `/Users/joaofnds/code/dotfiles/dot_agents/evals/instructions-reviewer/case-1-conformant/fixture/error-message-reviewer.md`

**Verdict:** Pass with revisions
**Tier:** sub-agent system prompt
**Size:** 85 lines / 30–150 for a sub-agent prompt (`artifact-class-checks.md` §Per-file budgets): within budget

Scope note: this file lives under `evals/.../fixture/` and nothing loads it to govern an agent, so it is a fixture, not live policy. The caller named it as a sub-agent definition, so I reviewed it as that class; the verdict is about the artifact's construction, not about any running agent's behavior.

## Findings

### Blocker

None.

### Major

None.

### Minor

1. **Three-value verdict with no assignment rule**: `dot_agents/evals/instructions-reviewer/case-1-conformant/fixture/error-message-reviewer.md`: `## Output`
   > **Verdict:** Pass / Pass with revisions / Fail

   **Why:** No-op / self-reference on a required output field: the prompt names no decision boundary anywhere for which of the three labels a given finding set earns, and this agent has no severity ladder to derive one from (its findings are uniform "missing criterion" entries). Two of the three labels are unreachable by rule, so the model resolves the verdict by vibe and the same input can return different verdicts across runs. The caller consumes the verdict as the review's completion signal.
   **Suggest:** Collapse to the two labels the finding model actually distinguishes and state the boundary on the same line:

   ```markdown
   **Verdict:** Pass / Fail: Fail when any string carries a finding.
   ```

   Changes for the consuming agent: the verdict becomes a function of the findings list rather than a judgment, so a caller can check it against the document body. Cost: deletes "Pass with revisions"; nothing is lost, because no text ever distinguished it from "Fail." Adds one clause, which takes nothing from the criteria bullets; it constrains only the output header.

Checked and not flagged, for the record: `tools: Read, Grep, Glob` is least-privilege for a reviewer and closes every mandated action (no `Bash`, and the prompt never mandates running `git diff`); the description front-loads the trigger and carries the house "Skip when" clause within the 1,536-character listing cap (`instruction_external_facts.md` §Harness mechanics); the completion gate at `## Done` is both checkable and exhaustive, with the "no findings still lists its strings" clause closing the premature-completion route; `## Input` closes caller-context leakage by naming the supplied input and requiring a one-line request when it is absent; the `<examples>` block is delimited and each of its three entries resolves a distinct criterion. The §7 embedded-verification check is diff-seed-only and does not apply to a standing review.

## Files examined

- `/Users/joaofnds/code/dotfiles/dot_agents/evals/instructions-reviewer/case-1-conformant/fixture/error-message-reviewer.md`: target: examined
- `/Users/joaofnds/code/dotfiles/dot_agents/rules/writing_instructions.md`: evidence (house bar): examined
- `/Users/joaofnds/code/dotfiles/dot_agents/agents/references/artifact-class-checks.md`: evidence (budget, §Sub-agent specifics): examined
- `/Users/joaofnds/code/dotfiles/dot_agents/agents/references/dispatch-fields.md`: evidence (tool-field semantics): examined
- `/Users/joaofnds/code/dotfiles/dot_agents/rules/instruction_external_facts.md`: evidence (§Harness mechanics: skill-listing cap, sub-agent `tools`): examined
- `/Users/joaofnds/code/dotfiles/dot_agents/rules/instruction_failure_modes.md`: evidence (failure-mode vocabulary): examined
