# 2026-08-17 (re-run): Case 2 (planted defects)

**Agent under test:** `dot_agents/agents/instructions-reviewer.md` at commit
`e5b76b47c947c290b39c1484a240bfbc08b062d1` (the `**Size:**` line-count requirement
deleted). Applied to `~/.agents/agents/instructions-reviewer.md` before the run.
**Answer key:** `case-2-planted-defects/CASE.md`, as corrected in `1911e4c`: P7's
expected severity is Major, citing `instruction_external_facts.md` §Deprecated model
mechanics.
**Invocation:** the `instructions-reviewer` agent, prompt verbatim from CASE.md §Input,
one run, no added scaffolding.

## Score

**Pass.** CASE.md §Scoring: 7/7 recall, P2 and P4 as Blockers, D1 not flagged, 0
manufactured findings.

- **Recall: 7/7.**
- **Severity accuracy: 7/7.**
- **Precision:** D1 not flagged as a defect. Three extra findings, all grounded in quoted
  fixture text. 0 manufactured.

### Plant-by-plant

| Plant | Expected | Found as | Severity |
| --- | --- | --- | --- |
| P1 stale `naming_conventions.md` | Major | Finding 4 | Major ✅ |
| P2 Output vs Handoff contradiction | Blocker | Finding 2 | Blocker ✅ |
| P3 description has no skip-when | Major | Finding 5 | Major ✅ |
| P4 `Edit` / `Write` / bare `Bash` on a reviewer | Blocker | Finding 1 | Blocker ✅ |
| P5 over-triggering | Major | Finding 5 (description doubt-clause) + finding 7 (body `ALWAYS` / `If in doubt`) | Major ✅ |
| P6 hedge rule + no completion gate | Major | Finding 6 (no completion criterion) + finding 9 (the hedge) | Major ✅ |
| P7 `budget_tokens` | Major | Finding 3 | Major ✅ |

P5 and P6 each landed split across two findings: the Major half where the plant's blast
radius is, the second instance as a Minor. P3 and P5's description halves are aggregated
into one finding, which the reviewer's own §How you review permits.

P7's severity now matches the corrected key: Major, citing
`instruction_external_facts.md` §Deprecated model mechanics with its last-checked state
(the Opus 5 / Fable 5 release pages). The earlier 2026-08-17 run ranked it Blocker.

### Secondary: process

| # | Expected | Observed |
| --- | --- | --- |
| 5 | P1's absence verified by a tool call this session | Yes: `Glob ~/.agents/rules/*.md` plus a corpus grep, both named in the finding |
| 6 | P7 cites the store's §Deprecated model mechanics as a severity-bearing finding | Yes, with the last-checked state; not an unverified note |
| 7 | Four parts per finding | All ten carry Quote, Severity (by section), Why with a named mechanism, and a concrete Suggest |

Every fixture claim in the run was re-verified against the fixture text: `tools` line,
`model: opus`, the `ALWAYS` / `If in doubt` sentences, the missing rules file, the
`budget_tokens` section, the Output/Handoff pair, the hedge bullet, and the dated
parenthetical. All hold. **0 fabricated citations.**

### D1, judged: not flagged

D1 (lines 17–18, the negative-only install ban) appears inside finding 1's reasoning as
evidence that prose is not enforcement while `Bash` is unscoped. It is not ranked as a
defect and the run never calls its negative-only framing wrong, so precision holds.

One nuance worth recording: finding 1's Suggest ends "if `Bash` stays, the ban belongs in
a `permissions.deny` rule in settings, and the body line should say so instead of standing
alone": a conditional edit to the decoy line, as part of the P4 remedy. It rests on the
enforcement-layer rule the reviewer already carries, not on the framing D1 was built to
protect. Scored as not-flagged; flagged here so the call is visible rather than silent.

### The dated pin rule (§Known judgement calls)

Finding 10 proposes deleting the `(Added 2026-03-04 …)` parenthetical and keeping the
rule. That is the defensible route the key describes: the rule is not called stale or
wrong, and the finding is ranked Minor. It reads as a prescription rather than a
suggestion, which the key cautions against, but the key's own text makes the Minor "not a
precision failure".

### Line count, again

The run reported `36 lines`; `wc -l` gives 35. Seventh consecutive one-high count, and
the second in this session. The `**Size:**` template no longer asks for a count: this run
volunteered one, in the old template's shape ("36 lines / 30–150 line budget for this
tier"). Cause unsettled between habit and the run loading a definition cached before the
apply. Probe: run a case first in a fresh session.

### Harness note

The output arrived with a harness prefix reporting that it matched an instruction-shaped
pattern (`permissions-allow-deny`) and that control tags were neutralized. The trigger is
finding 1's `permissions.deny` prescription. No content was lost; the prefix is not part
of the review and is excluded from the verbatim copy below.

## Run output, verbatim

# Review: `/Users/joaofnds/code/dotfiles/dot_agents/evals/instructions-reviewer/case-2-planted-defects/fixture/dependency-audit-reviewer.md`

**Verdict:** Fail
**Tier:** sub-agent system prompt
**Size:** 36 lines / 30–150 line budget for this tier (`artifact-class-checks.md` §Per-file budgets): within budget

## Findings

### Blocker

1. **Write-capable reviewer, and a mutation ban that nothing enforces**: `dependency-audit-reviewer.md`: frontmatter `tools`
   > `tools: Read, Grep, Glob, Edit, Write, Bash`

   **Why:** Over-privileged tools plus a false safety boundary. A reviewer with `Edit`/`Write` can rewrite the manifest it is auditing, and the one hard constraint in the body, "Never run a package manager's install or update command", rests on prose while `Bash` is unscoped; instruction text is not enforcement (`instruction_external_facts.md` §Harness mechanics, last full pass CLI 2.1.222: a sub-agent's `tools` restricts, but nothing inside the definition constrains what a granted `Bash` may run).
   **Suggest:** `tools: Read, Grep, Glob, Bash`. Delete `Edit` and `Write`. If no audit step needs a shell resolver command, drop `Bash` too and the mutation ban becomes structurally impossible rather than merely forbidden; if `Bash` stays, the ban belongs in a `permissions.deny` rule in settings, and the body line should say so instead of standing alone. Cost: the agent can no longer apply fixes it finds: correct for a reviewer, and it makes the inline-return contract in finding 2 the only reachable output path.

2. **Output and Handoff contradict each other**: `dependency-audit-reviewer.md`: §Output vs §Handoff
   > Never write it to a file

   > Write the finished audit to `.boris/reviews/dependency-audit.md`

   **Why:** Reachable self-contradiction resolved by vibe. Both sections are in the prompt on every invocation, no condition gates either, and the agent currently holds `Write`, so both branches are executable and the model picks one per run. Reachability probe the caller can run: invoke the agent on a repo with a manifest and check whether `.boris/reviews/dependency-audit.md` exists afterward; either outcome proves one section was silently ignored.
   **Suggest:** Delete §Handoff entirely (lines 32–35). It is the section that conflicts with the least-privilege fix in finding 1, its resume claim ("so the next session can pick up where this one stopped") imposes no action: the prompt never tells this agent to read that file back, and the fixed filename means every run overwrites the continuity it claims to create. Keep the inline return, rewritten per finding 5. Cost: nothing; `artifact-class-checks.md` §Sub-agent specifics prefers a file artifact only when the agent has a write tool and the pipeline crosses context boundaries, and after finding 1 it has neither.

### Major

3. **`budget_tokens` is a 400 on this model, and the agent cannot set it anyway**: `dependency-audit-reviewer.md`: §Reasoning budget
   > walking the transitive tree, so a thousand-package manifest cannot exhaust the window.

   > Cap the reasoning spend on a large lockfile by setting `budget_tokens` to 8000 before

   **Why:** Decay plus capability closure. `instruction_external_facts.md` §Deprecated model mechanics (last checked against the Opus 5 / Fable 5 release pages) records `budget_tokens` as returning 400 on Claude 4.7 and later, and the frontmatter pins `model: opus`; separately, `budget_tokens` is an API request parameter and no tool in the `tools` list can set it, so the mandated action is impossible whatever the model. The `8000` constant is also judgment displacement: the right walk depth tracks the lockfile, not a fixed number.
   **Suggest:** Delete §Reasoning budget (lines 22–25). If context exhaustion on huge lockfiles is the real worry, the replacement is a scope rule, not a knob: "Resolve the transitive paths the lockfile diff changed; name any subtree you did not walk." Cost: nothing lost: the deleted lines could never execute.

4. **Load-bearing dead reference**: `dependency-audit-reviewer.md`: §What to check
   > Naming of vendored or forked packages follows `~/.agents/rules/naming_conventions.md`.

   **Why:** Decay. `~/.agents/rules/naming_conventions.md` does not exist (verified this session: `Glob ~/.agents/rules/*.md` lists no such file; grep for `naming_conventions` across `~/.agents` hits only this fixture and eval results). The bullet delegates the entire naming standard to the missing file, so the check cannot be performed at all.
   **Suggest:** Delete the bullet. A standing review cannot recover which branches the missing file carried, so do not substitute a pointer to `coding_style.md`: it carries class and variable naming (`§Never the Impl suffix`), nothing about vendored or forked packages. If a house rule for fork naming exists, state it inline in one imperative sentence. Cost: nothing; an unfollowable bullet already contributed zero.

5. **Dispatch description over-triggers and names no skip condition**: `dependency-audit-reviewer.md`: frontmatter `description`
   > or whenever you are unsure whether a dependency change is safe.

   **Why:** Over-triggering. A doubt-clause in a description matches any dependency-adjacent turn, and the entry names no "skip when", so the dispatcher has no exclusion to weigh: the agent gets invoked on turns that only read a manifest.
   **Suggest:** Cut the doubt clause and add the exclusion: `description: Audits a project's dependency manifest and lockfile when a manifest or lockfile changed: added, upgraded, or removed dependencies, or a transitive version that resolved differently than the manifest declares. Skip when no manifest or lockfile is in the diff, and skip for a lockfile refresh with no version changes.` Cost: adds one skip clause the dispatcher must read; removes an unbounded trigger.

6. **No checkable, exhaustive completion criterion**: `dependency-audit-reviewer.md`: §Output
   > Return the audit inline as markdown. Never write it to a file: the caller wants it in the

   **Why:** Premature completion. The section fixes the format and nothing else, no required sections, no per-dependency accounting, no statement of what "done" covers, so the agent cannot tell a complete audit from three findings and a stop, and the caller cannot either.
   **Suggest:** Replace §Output with:

   ```markdown
   ## Output

   Return the audit inline as markdown, in this order:

   - **Audited**: every manifest and lockfile path you read.
   - **Findings**: one entry per dependency that failed a check above, naming the check
     and the manifest or lockfile line that shows it.
   - **Not resolved**: every transitive subtree you did not walk, with the reason.

   Every direct dependency in the manifest is accounted for: it appears under Findings or
   the Audited entry states it passed every check. A dependency you could not resolve goes
   under Not resolved, never omitted.
   ```

   Cost: three required sections the agent must fill, against a one-line format rule that gated nothing; the "accounted for" wording is the exhaustiveness form `artifact-class-checks.md` §Sub-agent specifics requires.

### Minor

7. **The body tells the agent to invoke itself**: `dependency-audit-reviewer.md`: opening paragraph
   > any dependency change reaches review. If in doubt, use it.

   **Why:** No-op addressed to the wrong reader. This text loads only *after* invocation, so no dispatcher ever sees it; it restates the description's trigger where it cannot route anything, and its "ALWAYS … proactively" / "If in doubt" framing is the anti-laziness form that over-triggers when it does reach a dispatcher.
   **Suggest:** Delete both sentences, keeping the role line "You audit dependency manifests and lockfiles." The dispatch condition lives in the description (finding 5): one home. Cost: nothing.

8. **No input contract**: `dependency-audit-reviewer.md`: §What to check
   > - Every direct dependency traces to a demonstrated requirement, not an assumed one.

   **Why:** Assumed shared context. The prompt never says how the audit target is identified, so in a polyglot or monorepo checkout the agent guesses which manifest the caller meant and reports on a subset without saying so.
   **Suggest:** Add one line above §What to check: "Audit the manifests and lockfiles the caller names. When the caller names none, audit every manifest tracked in the repo and list them all under Audited." Cost: one line; removes the guess and feeds the Audited section from finding 6.

9. **Vague hedge that names no action**: `dependency-audit-reviewer.md`: §What to check
   > - Try to be thorough and consider the security implications where appropriate.

   **Why:** No-op meta-rule: "try to," "consider," "where appropriate" name neither an action nor a checkable result, and it sits among four bullets that do, diluting them.
   **Suggest:** Delete the bullet. If the advisory check is meant to be real, it has a checkable form, "Flag every dependency the lockfile's audit command reports with a published advisory, with the advisory ID", which requires a named command; do not keep the sentiment version. Cost: nothing lost.

10. **Provenance narrative in loaded prose**: `dependency-audit-reviewer.md`: §What to check
    > floats. (Added 2026-03-04 after a floating minor bump silently changed TLS defaults.)

    **Why:** `writing_instructions.md` §No provenance in loaded prose: inline dates and incident narratives are paid on every load and belong in git history. The parenthetical supplies no scope, authority, or re-check trigger for the rule it follows.
    **Suggest:** Delete the parenthetical; keep the rule ("A dependency pinned to an exact version carries a comment naming what breaks when it floats."). Cost: nothing: the incident is recoverable from git history.

## Files examined

- `/Users/joaofnds/code/dotfiles/dot_agents/evals/instructions-reviewer/case-2-planted-defects/fixture/dependency-audit-reviewer.md`: target, examined (whole file). Note: this path is an eval fixture, reviewed as a sub-agent definition because the caller named it as the target; no live carrier loads it.
- `/Users/joaofnds/.agents/rules/writing_instructions.md`: evidence, examined
- `/Users/joaofnds/.agents/rules/instruction_failure_modes.md`: evidence, examined
- `/Users/joaofnds/.agents/rules/instruction_external_facts.md`: evidence, examined (§Harness mechanics, §Deprecated model mechanics, §Cited sources)
- `/Users/joaofnds/.agents/agents/references/artifact-class-checks.md`: evidence, examined (§Per-file budgets, §Sub-agent specifics)
- `/Users/joaofnds/.agents/agents/references/dispatch-fields.md`: evidence, examined
- `/Users/joaofnds/.agents/rules/coding_style.md`: evidence, sampled (grep for naming rules, to test a replacement pointer for finding 4)
- `/Users/joaofnds/.agents/rules/` (directory listing): evidence, examined (confirms `naming_conventions.md` absent)
