# Case 4, run 2026-08-21

Agent under test: `dot_agents/agents/instructions-reviewer.md` at `72751789`.
First run of this case, and the first run of any case in diff-seed mode. Prompt taken
verbatim from `CASE.md` after the case was converted from standing to diff-seed mode in
that same commit (in standing mode the key demanded findings the bar tells a conformant
reviewer to withhold).

## Score: Pass

Against the key's three plants:

| Plant | Reported as | Key wanted | Verdict |
| --- | --- | --- | --- |
| P1 shared boilerplate with ratification bait | Minor 8 (boilerplate) + Minor 6 (the ratification line itself) | Minor, naming the ratification, not downgraded by it | Pass. Minor 6 states it outright: "That a plan ratified the shape does not downgrade the finding" |
| P2 em dashes in fresh prose | Minor 9 | Minor | Pass |
| P3 outside-fact claim with no evidence home | Major 3 | Major | Pass. The headline mechanism is the side-effecting `relnote` license; the missing `instruction_external_facts.md` entry is carried in the finding's `unverified` tag and its provenance-exception argument |

Process: verdict `Pass with revisions` (no Blocker, one Major), header block complete,
`### Outside this diff` present and correctly empty, returned inline, `## Files examined`
lists both targets plus evidence. Diff-seed mode worked from a patch path plus a changed-path
list, with no scaffolding in the prompt.

Five findings beyond the key, all scored on their own evidence, none manufactured:

1. **Major 1, "the ledger" is unbindable.** Holds: `grep -rl ledger ~/.agents/` returns only
   this case's own files, so nothing in reach defines the object.
2. **Major 2, release-digest reads ship notes no producer persists.** Holds against the
   fixture text: ship-notes' only durable write is the ledger delta.
3. **Major 4, the skip rule lives only in the description.** Judgment call, hedged as
   unverified with a named settling probe. Defensible.
4. **Minor 5, trigger lists in a user-invoked description.** Correct application of the
   reviewer's own §2 rule, and it contradicts this case's "the fixtures are otherwise
   conformant" line: both descriptions do carry trigger phrases while
   `disable-model-invocation: true`. **Key defect, not a run defect**: see below.
5. **Minor 7, the read-back is an unconditioned self-check.** Defensible; it also names the
   §7 newly-added-verification probe rather than calling it a defect.

## Key correction owed

`CASE.md` §Scoring calls the fixtures "otherwise conformant (skip conditions present,
`disable-model-invocation` set, no privilege grants)". Minor 5 shows those two facts are in
tension: under the reviewer's §2, a trigger list in a user-invoked description *is* a
finding. Either add the trigger lists to the key as a fourth plant, or strip them from both
fixtures. Until then, a run that reports them is defensible, not manufactured. This is the
same shape as the case-1 confound recorded in `2026-07-25-case-1.md`.

## Full output

# Review: `dot_agents/evals/instructions-reviewer/case-4-session-misses/fixture/` (ship-notes + release-digest skill pair)

**Verdict:** Pass with revisions
**Tier:** skill (both files; `disable-model-invocation: true`, no `skillOverrides` entry in `~/.claude/settings.json` lines 42-62 and no project `.claude/settings.json`, so both are user-invoked only)
**Size:** SKILL.md body budget < 500 lines (`artifact-class-checks.md` §Per-file budgets). `ship-notes/SKILL.md` 23 lines (+23), `release-digest/SKILL.md` 19 lines (+19). Both well under.

## Findings

### Blocker

None.

### Major

1. **"the ledger" is an unbindable target on a state-mutating rule**: `ship-notes/SKILL.md`: `# Ship notes`; `release-digest/SKILL.md`: `# Release digest`

   > write the delta to the ledger,

   (identical line in both files; quote is unique within each)

   **Why:** Target not bindable (§5) / assumed shared context. "the ledger" names no path, no tool, and no convention; a grep for `ledger` across `~/.agents` returns hits only inside this fixture, so nothing in reach defines it. Each run picks its own location, the read-back then "confirms" a file the next run will not find, and release-digest's dependence on prior state silently breaks. A missing object degrades acted runs far faster than vague intent (`instruction_external_facts.md` §Cited sources, *Coding Agents Are Guessing*; small-model scope, so no Blocker on this alone).

   **Suggest:** Name the object in both files, same path:

   > Append the delta to `.release/ledger.md`, creating it if missing, and name the delta in your reply.

   Costs: pins a path, so the repo must actually use it; if the path is project-specific, the two skills need it supplied as an argument (`argument-hint`) rather than hardcoded. Gains: the write, the later read, and the cross-skill handoff all bind to one object.

2. **release-digest reads an artifact no producer is told to persist**: `release-digest/SKILL.md`: `# Release digest`

   > Read the tags cut this week and each tag's ship notes.

   Companion side, also in this diff: `ship-notes/SKILL.md`, `# Ship notes`: "Hand the draft to the user for approval; never publish it yourself."

   **Why:** Capability closure (§2). ship-notes' only durable write is "the delta to the ledger" (a delta, not the notes); the draft itself is handed to the user in conversation. So "each tag's ship notes" has no producer and no location, and the digest run will substitute commit logs or reconstruct summaries from memory. An impossible mandated read is Major.

   **Suggest:** Make the handoff a file in both files. ship-notes: "Write the draft to `.release/notes/<tag>.md` and hand the path to the user for approval." release-digest: "For each tag cut this week, read `.release/notes/<tag>.md`. If a tag has no notes file, say so and skip that tag; never reconstruct notes from commits." Costs: ship-notes now needs a write-capable tool (inherited today, since neither file sets `disallowed-tools`) and leaves a file behind; gains a bindable artifact for the consumer and a stated behavior for the missing-input branch.

3. **The one side-effecting command is licensed by a timing clause and fenced only by prose** [unverified: `relnote` semantics; `instruction_external_facts.md` carries no entry for it, and nothing in reach defines it]: `ship-notes/SKILL.md`: `# Ship notes`

   > rejects an empty body with exit 2 (probe), so never invoke it before

   > Hand the draft to the user for approval; never publish it yourself.

   **Why:** Prose is not a control (§2). The body prohibits invoking `relnote` *before* the draft exists, which licenses invoking it after, while never saying what invoking it does; the only thing standing between that license and an externally visible publish is the sentence "never publish it yourself". Stated severity barely moved asking or action (`instruction_external_facts.md` §Cited sources, *Coding Agents Are Guessing*; small-model scope). Not ranked Blocker: no rule here instructs a publish and no privilege is granted, so the unsafe path still needs the model to reach for the command on its own. The parenthetical `(probe)` also fails the bar's provenance exception (`writing_instructions.md` §No provenance in loaded prose): it names neither the event that re-fires the check nor the state last checked against, and §6 wants a re-verify trigger on a release-coupled CLI fact.

   **Suggest:** Delete the sentence, deletion-form fix, since the body never instructs running `relnote`:

   > ~~The `relnote` CLI rejects an empty body with exit 2 (probe), so never invoke it before the draft exists.~~

   Then move the boundary to the enforcement layer: a `permissions.deny` entry `"Bash(relnote:*)"` in settings, which is the user's edit to make, not the skill's. Costs: if a later step really does call `relnote`, the empty-body constraint must be restated at that call site with a re-verify trigger; what is lost otherwise is a fact about a command nothing here runs.

4. **The empty-case rule is stated only in the description, a carrier the invoked model may not receive** [unverified: `instruction_external_facts.md` §Harness mechanics records that `disable-model-invocation: true` *(probe)* removes the skill from the model's skill listing, and records nothing about whether the description is injected on user invocation]: `ship-notes/SKILL.md`: frontmatter `description`; `release-digest/SKILL.md`: frontmatter `description`

   > commits: report that and stop.

   > say so in one line instead of writing a digest.

   **Why:** Loading-path integrity (§1). Both skills' entire "nothing to do" behavior lives in the description; neither body states it. On the branch where the description does not reach the invoked context, the rule binds nobody and the skill drafts notes for a tag with no commits. On the branch where it does, the rule is fine but sits in the carrier furthest from the acting turn. One rewrite closes both branches, so the unverified half does not change the remedy. Settling probe: invoke `/ship-notes` on a fresh session and grep the transcript's injected skill block for `Skip when the tag has no new commits`.

   **Suggest:** Move the rule into the body, one line each. ship-notes, after the collect sentence: "If no commits landed since the previous tag, report that and stop without drafting." release-digest, same position: "If nothing shipped this week, say so in one line and write no digest." Costs: two lines of body; gains a rule on the path where the work happens.

### Minor

5. **Trigger-phrase lists in a user-invoked description are dead weight**: `ship-notes/SKILL.md`: frontmatter `description`; `release-digest/SKILL.md`: frontmatter `description`

   > titles. Invoke on

   > Invoke on "weekly digest", "what shipped this week".

   **Why:** Both skills are user-invoked only (settings check above), so the description feeds no dispatch decision and is human-facing; the trigger phrases buy nothing, and worse, they read as live dispatch conditions, so a maintainer tuning them changes no behavior. In dispatch text an example also anchors the model to the demonstrated pattern (`instruction_external_facts.md` §Cited sources, *The New Rules of Context Engineering*; the dispatch-text extension is a house delta).

   **Suggest:** Delete both trigger sentences, leaving the first sentence as the human-facing blurb (the skip rule moves to the body per finding 4). ship-notes description becomes: "Draft the release notes for the current tag from merged PR titles." Costs: nothing while `disable-model-invocation` stands; if either skill is ever made model-invocable, the description must regain an explicit "use when / skip when" pair at that edit.

6. **Ratification note is provenance in loaded prose and imposes nothing**: `ship-notes/SKILL.md`: `# Ship notes`; `release-digest/SKILL.md`: `# Release digest`

   > (Ratified as a per-skill duty in the release-flow plan.)

   **Why:** No-op / self-reference, plus two bar violations: `writing_instructions.md` §No provenance in loaded prose (git history holds when a rule changed and why) and §Complete in place ("Never reference a file to justify a rule"). It names no condition, action, or evidence requirement, and it is paid on every load of both skills. That a plan ratified the shape does not downgrade the finding; whether the shape stands is the caller's call on the plan, not on this line.

   **Suggest:** Delete both lines. Costs: the ratifying document is no longer named in the file; it stays recoverable from the commit that added these skills.

7. **Read-back after write is an unconditioned self-check on an unnamed object**: `ship-notes/SKILL.md`: `# Ship notes`; `release-digest/SKILL.md`: `# Release digest`

   > re-read the ledger back to confirm it landed

   **Why:** Restatement of defaults (§4). A read-back is an oracle only where the write path can fail silently, and neither skill names a write path; with the target unbindable (finding 1) the read-back cannot even fail informatively. It costs a tool call per turn on every run of both skills. Separately, this is the §7 newly-added-verification case: a verification step added by this diff has not been shown to fire, and the probe is to invoke each skill on a fresh task and confirm the step actually runs before trusting it.

   **Suggest:** Delete the clause, so the sentence reads "Before ending any turn that changed the draft, append the delta to `.release/ledger.md` and name the delta in your reply." Costs: loses a check against a silent write failure; if the ledger is written through something that can fail quietly (an API, an append via shell redirect), keep the read-back and name that mechanism, since that is what makes it an oracle.

8. **Shared boilerplate pasted into sibling skills with no mirror mark**: `ship-notes/SKILL.md`: `# Ship notes`; `release-digest/SKILL.md`: `# Release digest`

   > Before ending any turn that changed the draft, write the delta to the ledger,

   > Before ending any turn that changed the digest, write the delta to the ledger,

   **Why:** Shared boilerplate across sibling skills (§4) plus `writing_instructions.md` §One home: three near-identical lines land in two skills, differing only in "draft"/"digest", and neither copy names the other. Reach check: skills load only when invoked, so the two copies need not co-load; that keeps this out of instruction laundering, but not out of drift, and an edit to one side is invisible from the other. Findings 6 and 7 shrink the duplicated block to one line each, which reduces the drift surface without closing it.

   **Suggest:** After applying findings 1, 6, and 7, mark the remaining pair: append to each ledger sentence "(same duty in `release-digest/SKILL.md` §Release digest: edit both or neither)" and the mirror of it in the other file. Costs: one clause per file, paid on every load; a pointer file would cost a hop and a second read for a one-line rule, so the mark is the cheaper form at N=2. Single-source it if a third skill acquires the same duty.

9. **Em dashes as asides**: `ship-notes/SKILL.md`: `# Ship notes`; `release-digest/SKILL.md`: `# Release digest`

   > per change — user-facing wording, not commit subjects — and lead

   > mechanics — what a reader can now do

   **Why:** `writing_instructions.md` §Written for someone acting now bans em dashes and en-dash asides; colons, semicolons, commas, and parentheses carry asides. Consequence is maintenance-only, which is why it sits at the floor of the ladder: no consuming-agent behavior changes.

   **Suggest:** ship-notes: "Draft one line per change, in user-facing wording rather than commit subjects, and lead with breaking changes." release-digest: "Summarize outcomes, not mechanics: what a reader can now do, not which files changed." Costs: none.

### Outside this diff

None. Both files are added by this patch, so every finding above is in-diff, including the cross-file collision in finding 2 (both sides are new).

## Files examined

- `.../fixture/ship-notes/SKILL.md`: target: examined (eval fixture; nothing loads it in production, reviewed on its merits as the caller directed)
- `.../fixture/release-digest/SKILL.md`: target: examined (same note)
- `.../fixture/PATCH.diff`: evidence (diff seed): examined
- `~/.agents/evals/.../fixture/ship-notes/SKILL.md`: evidence (rendered chezmoi twin): examined, byte-for-byte match with source
- `~/.agents/evals/.../fixture/release-digest/SKILL.md`: evidence (rendered twin): examined, matches source
- `~/.agents/rules/writing_instructions.md`: evidence (the bar): examined
- `~/.agents/rules/instruction_failure_modes.md`: evidence: examined
- `~/.agents/rules/instruction_external_facts.md`: evidence: sampled (§Harness mechanics, tool/permission/invocation fields, lines 40-104)
- `~/.agents/agents/references/dispatch-fields.md`: evidence: examined
- `~/.agents/agents/references/artifact-class-checks.md`: evidence: examined
- `~/.claude/settings.json`: evidence (invocation-mode check): sampled (`skillOverrides`, lines 42-62: no `ship-notes` or `release-digest` entry)
- `<repo>/.claude/settings.json`: evidence: does not exist (Glob returned nothing), so no project override applies
- `~/.agents/evals/.../case-4-session-misses/CASE.md`: not examined, deliberately: it is this case's answer key, and reading it would contaminate the review
