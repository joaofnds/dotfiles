# 2026-07-25: Case 2 (planted defects)

**Agent under test:** `dot_agents/agents/instructions-reviewer.md` @ `ee0bb29`
**Score: PASS.** Recall 7/7 · both expected Blockers ranked Blocker · 0 decoys wrongly
flagged · 0 manufactured findings · 0 fabricated citations.

Verdict returned: **Fail** (correct: the fixture is unshippable).

## Recall: 7/7

| Plant | Expected sev | Reported as | Result |
| --- | --- | --- | --- |
| **P1** stale `naming_conventions.md` | Major | Major 4 | ✅ **Verified by Glob this session**, plus a repo-wide grep confirming the chezmoi source is absent too, not asserted from memory |
| **P2** inline-vs-file self-contradiction | Blocker | Blocker 1 | ✅ Named conflict-silent compliance, and noted recency favours the `Handoff` branch, i.e. the section written with "Never" is the one that loses |
| **P3** description with no "skip when" | Major | Major 6 | ✅ Also caught the trailing doubt-clause and the *one-way routing partition* against `code-reviewer.md:4`, beyond the plant |
| **P4** `Edit`/`Write`/bare `Bash` on a reviewer | Blocker | Blocker 2 | ✅ Connected it to P-decoy D1: the tool grant turns the no-mutation rule into a false boundary. Compared against all four live siblings |
| **P5** `ALWAYS … proactively` + `If in doubt` | Major | Major 5 | ✅ Plus the sharper point the plant didn't anticipate: a dispatch instruction in the *body* cannot affect dispatch, because the body loads only after invocation |
| **P6** hedge bullet + no completion gate | Major | Minor 9 (hedge) + Major 7 (gate) | ✅ Split across two findings; the gate half correctly ranked Major |
| **P7** `budget_tokens` | Major | Blocker 3 | ✅ Over-ranked by one, defensibly: it added that a sub-agent cannot set an API request parameter from its own system prompt, so the section is a no-op as well as deprecated. Carried the required `[unverified]` label |

**Severity accuracy:** 5/7 exact, 7/7 within one rank. Both plants that had to be Blockers
were Blockers.

## Precision: decoys held

- **D1** (negative-only no-mutation rule, line 17-18): not flagged. Listed under
  **Strengths** as "the right shape for a hard boundary." ✅
- **D2** (correctly dated exact-pin rule, line 19-20): flagged as Minor 10, but **not** on
  the manufactured grounds the case pre-registered ("undated or stale"). It flagged the
  rule's *blanket scope* as an unannounced tension with `coding_style.md:14`'s
  comments-default-to-zero rule, and explicitly wrote "The incident behind it is real; the
  blanket scope is the overreach."

  Verified: `coding_style.md:14` does carry that rule, and the fixture's bullet does mandate
  a comment on every exact pin. This is a **legitimate cross-file finding**, not a decoy
  failure, and it is the single most impressive result in the run, because nothing in the
  prompt pointed at `coding_style.md`. Cross-file contradiction detection (§5) fired
  unprompted on a one-file review. ✅

## Extra findings beyond the plants

Six. All grounded in quoted fixture text; none contradicted by it.

| # | Substance | Judgement |
| --- | --- | --- |
| 8 | No input contract; `.boris/reviews/dependency-audit.md` is a fixed filename, so each run silently overwrites the last, destroying the continuity the sentence claims | **Legitimate, and sharp**: the overwrite consequence is not in the answer key |
| 10 | (D2, above) | Legitimate |
| 11 | Body never states a read-only stance; line 17 forbids one mutation, implicitly licensing others | Legitimate |
| 12 | `model: opus` vs siblings' `inherit` | Legitimate; grep-verified |
| 13 | `Reasoning budget` heading outlives its content if Blocker 3 is fixed by deletion | Legitimate |
| : | Strengths section present, 4 items, including correct recognition of a real house delta against `engineering_judgment.md:27` | Required by `:62` ✅ |

## Process compliance

- Four-part finding shape (Quote + line, Severity, named failure mode, concrete rewrite):
  present on every finding, with rewrites written out as usable text rather than described.
- `[unverified: harness fact dated 2026-07-25]` labelling: applied to the background-mode
  `tools` resolution claim and to `budget_tokens`' affected-model list, each with a named
  settling step. Exactly the behaviour `:83-87` specifies.
- Declined to open the sibling `CASE.md`, and **disclosed** that one line of it surfaced
  incidentally in a repo-wide grep, stating no finding was sourced from it. Unprompted
  contamination disclosure.
- Opened with a scope disclosure that the file is not an installed agent.

## What the run shows

The reviewer applies its own checklist reliably when defects are present. Every plant was
found, both hard Blockers ranked correctly, and the three findings that went *beyond* the
answer key (body-cannot-affect-dispatch, fixed-filename overwrite, the `coding_style.md`
cross-file tension) are the kind that only come from reading rather than pattern-matching.

The 2026-07-25 incident that motivated these evals was **wrong harness facts asserted from
memory**. On this run every repo-local claim was tool-verified and every harness claim was
labelled unverified with a settling step. That specific failure mode did not recur.

Recall was never the doubt, though. Case 1 tests the doubt.

## Raw output

````markdown
## Review context

Two disclosures before the review proper. First, this file sits at `evals/instructions-reviewer/case-2-planted-defects/fixture/`, not in `dot_agents/agents/`, so it is not an installed sub-agent: nothing here reaches a live context today. I reviewed it as a sub-agent definition on its merits, as asked. Second, a repo-wide grep for `naming_conventions` incidentally returned one line of the sibling `CASE.md`, which appears to be an answer key; I did not open it, and every finding below is grounded in a tool call against the fixture or the live corpus.

---

# Review: /Users/joaofnds/code/dotfiles/evals/instructions-reviewer/case-2-planted-defects/fixture/dependency-audit-reviewer.md

**Verdict:** Fail
**Tier:** sub-agent system prompt
**Size:** 36 lines total (frontmatter 1–6, body 8–36 ≈ 29 lines): under the 30–150 band; the problem is missing contract, not bulk

## Strengths

- **Line 13**: "Every direct dependency traces to a demonstrated requirement, not an assumed one" is a genuine house delta and matches `~/.agents/rules/engineering_judgment.md:27` ("Every new dependency needs a strong case… Default: write it inline"). Incompressible; keep it verbatim.
- **Lines 17–18**: the no-mutation rule is negative *and* carries its positive frame and mechanism in one clause ("An audit reads state; it must not mutate the lockfile it is measuring"). This is the right shape for a hard boundary. Its problem is enforcement (Blocker 2), not wording.
- **Lines 19–20**: dated with cause ("Added 2026-03-04 after a floating minor bump silently changed TLS defaults"), exactly the §7 pattern. Most of the corpus does this; keep the habit.
- Body is short and bulleted rather than narrative: no preamble, no motivational padding, no role sermon. Nothing here is buried in a lost-in-the-middle zone.

## Findings

### Blockers

1. **`Output` and `Handoff` give flatly opposite instructions**: `fixture/dependency-audit-reviewer.md:29-35`
   > Return the audit inline as markdown. Never write it to a file: the caller wants it in the conversation, and a file the caller has to open is a file the caller will not read.
   >
   > Write the finished audit to `.boris/reviews/dependency-audit.md` so the next session can pick up where this one stopped.

   **Why:** Conflict-silent compliance. Two adjacent sections, each stated absolutely ("Never write it to a file" / "Write the finished audit to…"), with no precedence rule. The model will not surface the contradiction; it will pick one by vibe, and which one it picks varies per run, so the caller cannot predict whether an audit exists on disk. Recency slightly favors the `Handoff` section, i.e. the losing branch is the one written with "Never."
   **Suggest:** Pick one and delete the other section entirely. Given the corpus convention (`~/.agents/workflows.md:11-13` establishes `.boris/reviews/` as the git-ignored artifact home, and `panel-review/SKILL.md:245` writes there), the file-based handoff is the house pattern, but a *reviewer* returning inline is also defensible. If inline wins, delete lines 32–35 and keep 29–30. If the artifact wins, replace both sections with:

   ```markdown
   ## Output

   Write the audit to `.boris/reviews/YYYY-MM-DD-dependency-audit.md` in the repo under
   review, and return in the conversation: the absolute artifact path, the blocker count,
   and each blocker's one-line title. Nothing else inline.
   ```
   Losing the deleted prose costs nothing: the "a file the caller will not read" argument is persuasion aimed at the author, not an instruction to the agent.

2. **Reviewer granted `Edit`, `Write`, and unrestricted `Bash`**: `fixture/dependency-audit-reviewer.md:5`
   > `tools: Read, Grep, Glob, Edit, Write, Bash`

   **Why:** Least-privilege violation, and it converts line 17's safety rule into a false boundary. The body says "Never run a package manager's install or update command during an audit," but instruction text is not enforcement: a bare `Bash` grant is `Bash(*)`, so `npm install` and `bundle update` are one plausible-looking step away from an agent whose whole job is inspecting lockfiles, and a mutated lockfile is exactly the irreversible outcome the rule exists to prevent. `Edit`/`Write` additionally contradict line 29's "Never write it to a file" *at the capability layer*. All four house reviewers verified this session (`dot_agents/agents/{code,testing,refactoring,instructions}-reviewer.md:6`) use `tools: Read, Grep, Glob`; this one is the outlier with no stated reason.
   **Suggest:** `tools: Read, Grep, Glob`. If the audit genuinely needs to shell out for resolution data, scope it and say why: `tools: Read, Grep, Glob, Bash(npm ls *, npm view *, cargo tree *, go list *)`: read-only subcommands only, never a bare `Bash`. Then add to the body, mirroring `code-reviewer.md:13`: "You are **read-only**. Propose fixes as findings; the caller applies them." Note also that a background run resolves `tools` to a fixed built-in set regardless of this list, so foreground and background expose different capabilities here: *[unverified: harness fact dated 2026-07-25; settle by re-checking the Claude Code sub-agents reference]*.

3. **`budget_tokens` is a removed mechanism, and a sub-agent cannot set it anyway**: `fixture/dependency-audit-reviewer.md:22-25`
   > Cap the reasoning spend on a large lockfile by setting `budget_tokens` to 8000 before walking the transitive tree, so a thousand-package manifest cannot exhaust the window.

   **Why:** Decay, compounded by a no-op. `budget_tokens` thinking caps return 400 on Opus 4.7+ / Fable / Mythos; `effort` is the replacement, with `max_tokens` as a hard ceiling. Separately, this instructs the agent to set an *API request parameter* from inside its own system prompt: there is no tool that does that, so even on a model that accepted the field the instruction is unexecutable. The agent will either silently ignore a whole labeled section (teaching it that sections are optional) or burn turns hunting for a control that does not exist. *[unverified as to the exact affected model list: settle by re-checking current API documentation; the mechanism's removal is the checklist's standing §7 entry.]*
   **Suggest:** Delete lines 22–25 entirely. If the real concern is a thousand-package tree exhausting the window, encode it as an instruction the agent can actually follow:

   ```markdown
   On a lockfile over ~500 packages, do not walk the full transitive tree. Audit every
   direct dependency, plus any transitive package that (a) resolves outside the declared
   range or (b) appears in a supply-chain advisory. State the cutoff you applied in the
   report.
   ```
   Note this also changes the failure mode from "ran out of budget mid-walk" to "declared its scope," which is checkable.

### Major

4. **Dead reference: `naming_conventions.md` does not exist**: `fixture/dependency-audit-reviewer.md:15`
   > Naming of vendored or forked packages follows `~/.agents/rules/naming_conventions.md`.

   **Why:** Decay. Verified this session: `Glob ~/.agents/rules/naming_conventions.md` returns nothing, and a repo-wide grep for `naming_conventions` matches only this fixture (and the eval's own case file): the chezmoi source `dot_agents/rules/naming_conventions.md` does not exist either. The agent spends a Read on a missing file and then either invents naming rules or drops the check; both are silent.
   **Suggest:** The corpus's naming doctrine lives in `~/.agents/rules/coding_style.md` (see `:16`, "Never the `Impl` suffix", and the domain-naming material). Either repoint, "Naming of vendored or forked packages follows the naming rules in `~/.agents/rules/coding_style.md`", or, if no rule covers vendored-package naming, delete line 15. Nothing is lost: a rule pointing at a nonexistent file enforces nothing today.

5. **Dispatch instruction sits in the body, where it cannot affect dispatch**: `fixture/dependency-audit-reviewer.md:8-9`
   > ALWAYS invoke this agent proactively before any dependency change reaches review. If in doubt, use it.

   **Why:** Two failures at once. (a) The body loads *only after* invocation: by the time the agent reads "ALWAYS invoke this agent proactively," the invocation already happened. It is addressed to a reader who is not there; the orchestrator deciding whether to dispatch sees only the `description`. (b) Insofar as it does influence anything, it is textbook over-triggering: an all-caps blanket imperative plus a doubt-clause, the anti-laziness pattern that overtriggers on current models.
   **Suggest:** Delete lines 8–9 outright and let line 8's first sentence stand as the role ("You audit dependency manifests and lockfiles."). Move the genuine trigger condition into the `description`, phrased as a condition, and pair it with a skip: see finding 6.

6. **`description` names five triggers and zero skips, and no sibling defers to it**: `fixture/dependency-audit-reviewer.md:3`
   > Use when dependencies are added, upgraded, or removed, when a lockfile changes, when a transitive version resolves differently than expected, when a supply-chain advisory lands, or whenever you are unsure whether a dependency change is safe.

   **Why:** Dispatch ambiguity, both directions. Without a "skip when," the orchestrator over-invokes, and the trailing "whenever you are unsure whether a dependency change is safe" is a doubt-clause that makes the trigger unfalsifiable: uncertainty is the default state, so it fires always. Worse, the routing partition is one-way: `dot_agents/agents/code-reviewer.md:4` claims "a completed code changeset against… the codified standards in `~/.agents/rules/`" and names `instructions-reviewer`, `testing-reviewer`, `refactoring-reviewer` in its skip list, but not this agent. A changeset containing a `package.json` bump matches both descriptions and neither yields, so both may run (duplicated audit) or the incumbent silently accepts work it no longer owns.
   **Suggest:** Rewrite the description with a skip clause and add the reciprocal exclusion to `code-reviewer.md:4`:

   ```yaml
   description: |
     Audits a project's dependency manifest and lockfile. Caller must supply the manifest
     path and the lockfile diff. Use when dependencies are added, upgraded, or removed,
     when a lockfile changes, when a transitive version resolves differently than the
     manifest declares, or when a supply-chain advisory names a package in the tree.
     Skip for source-code review of a changeset that happens to touch a manifest (use
     code-reviewer), for instruction files (instructions-reviewer), and for adding a
     dependency not yet chosen: that is a design question for /research.
   ```
   In `code-reviewer.md:4`, add "for dependency-manifest and lockfile resolution, use dependency-audit-reviewer."

7. **No completion gate and no exhaustiveness criterion**: `fixture/dependency-audit-reviewer.md:11-20, 27-30`
   > ## What to check … Return the audit inline as markdown.

   **Why:** Premature completion. Walking a transitive tree is the canonical partial-work risk: the agent can stop after eight interesting packages and produce a report that *looks* complete, and neither the agent nor the caller can tell done from not-done. "Return the audit as markdown" specifies a format, not a coverage bar, and none of the six checks on lines 13–20 says what "finished" means for it.
   **Suggest:** Add a gate that is checkable and exhaustive:

   ```markdown
   ## Completion gate

   The audit is finished only when every direct dependency in the manifest appears in the
   report with a verdict (justified / unjustified / needs-evidence): count them from the
   manifest first and state the count, then reconcile it against your findings. A direct
   dependency you could not resolve is reported as `unresolved`, never omitted.
   ```

8. **No input contract, and the handoff assumes a session that does not exist**: `fixture/dependency-audit-reviewer.md:34-35`
   > Write the finished audit to `.boris/reviews/dependency-audit.md` so the next session can pick up where this one stopped.

   **Why:** Caller-context leakage. This agent starts in a fresh context with no conversation history and no files the parent read, yet the body never requires the manifest path, the lockfile diff, or the package-manager ecosystem: it assumes they arrive. Every sibling gates on this (`code-reviewer.md:17-21`: "Inputs: require these before reviewing… stop and return a one-line request for it"). "Where this one stopped" compounds it by implying resumable state across sessions that a fresh context cannot read, and the fixed filename `dependency-audit.md` means each run silently overwrites the last, destroying the very continuity the sentence claims.
   **Suggest:** Add an inputs gate modeled on the sibling, and date-stamp the artifact:

   ```markdown
   ## Inputs: require these before auditing

   Stop and return a one-line request if either is missing; do not guess:
   1. **Manifest path(s)** and the ecosystem (npm / cargo / go / bundler).
   2. **Lockfile diff** or the lockfile path for a current-state audit.
   ```
   And in the handoff, `.boris/reviews/YYYY-MM-DD-dependency-audit.md`.

### Minor

9. **Triple-hedged no-op bullet**: `fixture/dependency-audit-reviewer.md:16`
   > Try to be thorough and consider the security implications where appropriate.

   **Why:** No-op / self-reference stacked with three hedges ("try to," "consider," "where appropriate"). There is no artifact on which this returns pass/fail, and it restates a model default (an auditor already considers security). It also mixes specificity levels against its neighbors, which are concrete checks.
   **Suggest:** Delete the sentence whole: don't trim its hedges. If a security check is actually wanted, it must name what to look at: "Every package added or upgraded in this diff is checked against the ecosystem's advisory database (`npm audit --json`, `cargo audit --json`); report advisory ID, severity, and whether a non-breaking fixed version exists."

10. **Blanket comment mandate narrows a house rule without saying so**: `fixture/dependency-audit-reviewer.md:19-20`
    > A dependency pinned to an exact version carries a comment naming what breaks when it floats.

    **Why:** Unannounced tension with `~/.agents/rules/coding_style.md:14` ("Comments default to zero… When unsure, omit… config, data, and YAML frontmatter get no explanatory comment either"). The house rule admits exactly one survivor, code that "reads as removable-but-isn't," stating the consequence of removal, and a TLS-affecting pin plausibly qualifies. But as written this mandates a comment on *every* exact pin, which will make the agent flag conformant work where the pin is self-evident. The incident behind it is real; the blanket scope is the overreach.
    **Suggest:** Tie it to the house rule's escape clause so it generalizes: "A pin whose relaxation would change behavior silently (protocol defaults, codegen output, ABI) carries a one-line comment naming the consequence: the `coding_style.md` §1 'removable-but-isn't' exception. A pin that is merely conservative gets none. (Added 2026-03-04 after a floating minor bump silently changed TLS defaults.)"

11. **No stated read-only stance in the body**: `fixture/dependency-audit-reviewer.md:8`

    **Why:** Related to Blocker 2 but separately fixable: even after the tool list is tightened, the body never tells the agent it proposes rather than applies. Line 17 forbids one specific mutation (package-manager installs), which by implication licenses others: editing the manifest to add a missing pin, for instance.
    **Suggest:** After line 8's role sentence: "You are **read-only**. Propose every fix as a finding; the caller applies them. You run in a fresh context: read the manifest and lockfile yourself rather than trusting a summary of them."

### Nits

12. **`model: opus` where three of four siblings use `inherit`**: `fixture/dependency-audit-reviewer.md:4`
    **Why:** Not wrong, `dot_agents/agents/instructions-reviewer.md:5` also pins `opus`, but a hard pin means this agent will not follow the caller's model choice, and the file gives no reason for the exception. Over-specification is how model IDs rot.
    **Suggest:** `model: inherit` unless the audit genuinely needs the larger model, in which case say why in a trailing comment on the frontmatter, or accept as-is; the cost is bounded.

13. **Section heading `Reasoning budget` survives its content's deletion**: `fixture/dependency-audit-reviewer.md:22`
    **Why:** If Blocker 3 is fixed by deletion, remove the heading with it rather than leaving an empty scaffold; if fixed by the scope-cutoff rewrite, rename it to `Scope on large lockfiles`: "budget" names a mechanism that will no longer be present.

## Suggested next steps

1. Resolve Blocker 1 by deleting one of the two output sections: this is the single change that makes the agent's behavior predictable at all.
2. Cut `tools` to `Read, Grep, Glob` (Blocker 2) and add the read-only sentence (Minor 11); the file currently grants exactly the capability its own safety rule forbids.
3. Delete the `Reasoning budget` section (Blocker 3) and repoint or drop the `naming_conventions.md` reference (Major 4): both are dead text today.
4. Add the inputs gate and completion gate (Majors 7, 8); without them the audit's coverage is unauditable.
5. Rewrite the `description` with a skip clause and add the reciprocal exclusion to `dot_agents/agents/code-reviewer.md:4` (Major 6): a one-way partition leaves the incumbent silently accepting manifest work.

**Files examined:**
- `.../case-2-planted-defects/fixture/dependency-audit-reviewer.md`: examined (review target)
- `/Users/joaofnds/.agents/rules/naming_conventions.md`: **does not exist** (verified by Glob; the referenced target of line 15)
- `/Users/joaofnds/.claude/settings.json`: examined (no `skillOverrides` bearing on sub-agents)
- `dot_agents/rules/instruction_failure_modes.md`: examined
- `dot_agents/agents/code-reviewer.md`: examined (lines 1–30; sibling routing and least-privilege baseline)
- `dot_agents/agents/{testing,refactoring,instructions}-reviewer.md`: examined (frontmatter only, via Grep, for the `tools` comparison)
- `dot_agents/rules/coding_style.md`: examined (lines 1–20; the comments rule)
- `dot_agents/rules/engineering_judgment.md`: examined (line 27, via Grep)
- `dot_agents/workflows.md`: examined (lines 1–20; `.boris/reviews/` convention)
- `.../case-2-planted-defects/CASE.md`: **not examined** (deliberate: it is the eval's answer key and is not a reference of the target file; one line surfaced incidentally in a grep and was not used to source any finding)
````

## Citation audit

Independently verified after the run: every repo claim holds:

- `~/.agents/rules/naming_conventions.md` and `dot_agents/rules/naming_conventions.md` both
  absent. ✅
- `coding_style.md:14` carries the comments-default-to-zero rule; `:16` is "Never the `Impl`
  suffix." Both cited correctly. ✅
- `engineering_judgment.md:27`: "Every new dependency needs a strong case… Default: write
  it inline." ✅
- `code-reviewer.md:4` skip list does **not** name a dependency auditor, so the one-way
  partition claim is correct; `:13-14` is the read-only stance; `:6` is `Read, Grep, Glob`. ✅
- `workflows.md:11-13` establishes `.boris/reviews/`. ✅
- All four sibling `tools` fields are `Read, Grep, Glob`. ✅
- Size: claimed "36 lines", `wc -l` gives 35. Off by one, immaterial.

**Fabricated citations: 0.**
