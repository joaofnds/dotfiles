# 2026-08-17: Case 2 (planted defects)

**Agent under test:** `dot_agents/agents/instructions-reviewer.md` at commit
`9ababda748fadcd6194df73969246b0bf430f72c` (the reviewer rewrite landed in `1bfc61d`).
**Answer key:** `case-2-planted-defects/CASE.md`, re-derived 2026-08-17.
**Invocation:** the `instructions-reviewer` agent, prompt verbatim from CASE.md §Input,
one run, no added scaffolding.

## Score

**Pass.**

- **Recall:** 7/7. Every plant reported with a matching mechanism.
- **Severity accuracy:** 6/7. P7 was ranked Blocker where the key expects Major.
  P2 and P4 landed as Blockers as required.
- **Precision:** D1 not flagged. 0 manufactured findings: all 8 findings map to a plant
  or to the key's declared judgement call.

Pass conditions from CASE.md §Scoring: 7/7 recall ✓, P2 and P4 as Blockers ✓, D1 not
flagged ✓, 0 manufactured findings ✓.

### Plant-by-plant

| # | Reported as | Expected severity | Given | Note |
| --- | --- | --- | --- | --- | --- |
| P1 | Finding 4, dead reference | Major | Major | Absence verified this session by Glob over both `~/.agents/rules/` and `dot_agents/rules/`: process item 5 satisfied |
| P2 | Finding 2, contradictory destinations | Blocker | Blocker | Carried a reachability probe: both rules unconditional, no branch satisfies one without violating the other |
| P3 | Finding 6 (merged with P5) | Major | Major | "The description names no 'skip when' condition, so its scope has no lower edge" |
| P4 | Finding 1, over-privileged tools | Blocker | Blocker | Also names the false-safety-boundary angle: the prose no-install rule is not enforcement |
| P5 | Finding 6 (merged with P3) | Major | Major | Both the `ALWAYS ... proactively` and the doubt-clause quoted |
| P6 | Findings 5 and 7 (split) | Major | Major (gate) + Minor (hedge) | Completion-gate half at Major, hedge half at Minor |
| P7 | Finding 3, `budget_tokens` | Major | **Blocker** | Cited `instruction_external_facts.md` §Deprecated model mechanics as process item 6 requires |

### Decoy

D1 (line 17-18, the no-install safety rule) was **not** flagged as a defect. It appears
inside finding 1 as evidence that a prose rule is not an enforcement boundary against the
granted `Bash`: an argument about the tools line, not about D1's negative-only framing.
Precision holds.

### Known judgement call

The dated exact-pin rule (line 19-20) came back as finding 8, Minor, proposing the date
and incident narrative move to git history. CASE.md §Known judgement calls marks this
defensible and not a precision failure. One deviation from the key's caveat: the key says
the reviewer "should not demand the change, only suggest it", and the finding is headed
"Bar violation": it sits under Minor and its remedy is a `Suggest:`, but the framing
asserts the bar binds standing fixture prose, which the bar's own scope sentence does not.

## Answer-key notes for a human read

1. **P7's expected severity looks low.** The run's argument for Blocker is that the
   fixture declares `model: opus` and `budget_tokens` 400s on that model, so the
   instruction is unexecutable on the declared model, not merely stale. The key's Major
   predates the store entry naming Opus 5 explicitly. Not scored against the run: the
   pass condition names only P2 and P4.
2. **Process item 7 asks for something the current reviewer will not produce.** It
   requires each finding to carry "Quote (with line)". This run quoted with section
   names (`frontmatter`, `§Output`, `§What to check`) and no line numbers, which is what
   `writing_instructions.md` and the corpus budget script's citation-hygiene check now
   require. Item 7 is unsatisfiable as written.
3. **P6 is one row in the key but two defects in the fixture**: a hedge bullet and a
   missing completion gate. The run split them across severities. Scoring the row as a
   single Major hides that the hedge half came back Minor.

## Full output

Recorded verbatim below.

---

# Review: `/Users/joaofnds/code/dotfiles/dot_agents/evals/instructions-reviewer/case-2-planted-defects/fixture/dependency-audit-reviewer.md`

**Verdict:** Fail
**Tier:** sub-agent system prompt
**Size:** 36 lines / 30–150 (`artifact-class-checks.md` §Per-file budgets): within budget

Scope note: this file is an eval fixture, not a definition any router loads. The caller named it as a standing-artifact target, so it gets a verdict; nothing in the corpus changes if it is left as-is, and the gate must not be fired on it (`~/.agents/AGENTS.md` §Task lifecycle: "Never fire the gate on a fixture under `evals/`").

## Findings

### Blocker

1. **Reviewer holds `Edit`, `Write`, and unscoped `Bash`; the no-mutation rule is prose, not a boundary**: `dependency-audit-reviewer.md`: frontmatter
   > `tools: Read, Grep, Glob, Edit, Write, Bash`

   **Why:** Over-privileged tools plus a false safety boundary. §2 Least privilege: reviewers must not have `Edit`/`Write`, and `Bash` unscoped is a smell. The prompt's own safety rule, "Never run a package manager's install or update command during an audit", is instruction text, which `~/.agents/agents/instructions-reviewer.md` §2 states is not enforcement at all; the granted `Bash` makes `npm install` reachable in exactly the case the rule fears, and `Edit`/`Write` make the lockfile it is "measuring" writable.
   **Suggest:** `tools: Read, Grep, Glob`. Costs the agent nothing it needs: every check in §What to check is a read of manifest/lockfile text. If a resolver query is genuinely required, scope it (`Bash(npm ls:*)`) rather than granting `Bash`, and back it with a permission deny rule; frontmatter alone is not the enforcement layer.

2. **Output and Handoff give contradictory, both-reachable destination rules**: `dependency-audit-reviewer.md`: §Output
   > Never write it to a file: the caller wants it in the

   Colliding line, same file, §Handoff:
   > Write the finished audit to `.boris/reviews/dependency-audit.md` so the next session can

   **Why:** Reachable self-contradiction resolved by vibe. Reachability probe: both rules are unconditional and fire on every completed audit: there is no branch that satisfies one without violating the other, so every run hits the conflict. The agent will silently pick one, and the caller cannot predict which; `disable-model-invocation`-style routing does not disambiguate it.
   **Suggest:** Delete §Handoff entirely and keep the inline return. The deletion also removes the caller-context leak in "so the next session can pick up where this one stopped": a fresh sub-agent has no next session and no knowledge of where a prior one stopped. What is lost: nothing; §Output already states the destination and its one-sentence reason. (If the house wants the artifact instead, delete §Output's prohibition and drop `Write` back in, but §7's file-handoff test prefers the inline return here, since this pipeline returns to the calling conversation, not across a context boundary.)

3. **`budget_tokens` returns 400 on the declared model**: `dependency-audit-reviewer.md`: §Reasoning budget
   > Cap the reasoning spend on a large lockfile by setting `budget_tokens` to 8000 before

   **Why:** Decay, plus a capability-closure failure. `instruction_external_facts.md` §Deprecated model mechanics (last checked against the Opus 5 / Fable 5 release pages) records `budget_tokens` as a 400 on Claude 4.7 and later, naming Opus 5 explicitly, and this definition declares `model: opus`. Separately, the agent has no declared tool that sets a thinking budget, so the instruction mandates an action it cannot perform; the model burns attention on it and proceeds with an unmet mandate.
   **Suggest:** Delete §Reasoning budget (both lines). What is lost: nothing enforceable: the replacement mechanic (`thinking: {type: "adaptive"}` plus `output_config: {effort: ...}`) is a caller-side API parameter, not something a sub-agent body can set. If the real concern is a thousand-package tree, state it as a scoping rule the agent *can* execute: "On a lockfile over N packages, audit only packages on the changed dependency's resolution path and say so in the report."

### Major

4. **Dead reference carries the naming rule**: `dependency-audit-reviewer.md`: §What to check
   > Naming of vendored or forked packages follows `~/.agents/rules/naming_conventions.md`

   **Why:** Load-bearing dead reference. Verified this session: `~/.agents/rules/` contains no `naming_conventions.md` (Glob of `/Users/joaofnds/.agents/rules/*.md` and of `dot_agents/rules/*.md`). The bullet's entire content is the pointer, so the rule evaluates to nothing and the agent invents a naming standard or skips the check.
   **Suggest:** Delete the bullet. What is lost: nothing that exists. If a house naming rule for vendored packages is wanted, state it in place and complete; `writing_instructions.md` §Complete in place forbids referencing a file to justify a rule anyway.

5. **No checkable completion gate and no output contract**: `dependency-audit-reviewer.md`: §Output
   > Return the audit inline as markdown.

   **Why:** Premature completion. §7 requires a completion criterion the agent can check and, where partial work is the risk, an exhaustive one: a transitive tree is the archetypal partial-work risk. "Markdown" fixes no required sections, no severity vocabulary, and no bound, so the caller cannot tell a complete audit from three spot-checks.
   **Suggest:** Replace the first sentence with: "Return the audit inline as markdown, with one section per changed dependency and a `Verdict: safe / unsafe / unverified` line. Every dependency added, removed, or version-changed in the diff is accounted for by name; name any you could not resolve and why." Cost: adds one enumeration requirement to a body that currently states none, and makes the §What to check bullets checkable per dependency rather than in the abstract.

6. **Description and body both overtrigger; no skip clause**: `dependency-audit-reviewer.md`: frontmatter `description`
   > or whenever you are unsure whether a dependency change is safe

   Same mechanism in the body, §opening:
   > ALWAYS invoke this agent proactively before

   **Why:** Over-triggering. A doubt-clause plus a blanket `ALWAYS ... proactively` is anti-laziness prompting written for older models; on current models it fires the agent on unrelated changes. The body copy is also misplaced: a sub-agent body is read *after* dispatch, so an invocation instruction there changes no action (no-op), and it can only leak into the report as self-justification. The description names no "skip when" condition, so its scope has no lower edge.
   **Suggest:** Delete the body's two dispatch sentences ("ALWAYS invoke … If in doubt, use it."), keeping "You audit dependency manifests and lockfiles." In the description, drop the doubt-clause and add the complement: `… when a supply-chain advisory lands. Skip when no manifest or lockfile changed, or when the change is a pin comment or a workspace-internal path dependency.` Cost: the skip clause spends dispatch characters (cap 1,536/entry, `instruction_external_facts.md` §Harness mechanics, no measurement needed, this entry is far under) and buys back the routing edge the doubt-clause erased.

### Minor

7. **Hedge bullet names no action**: `dependency-audit-reviewer.md`: §What to check
   > Try to be thorough and consider the security implications where appropriate.

   **Why:** No-op meta-rule. "Try to," "consider," and "where appropriate" name neither an action nor a checkable result; `writing_instructions.md` §Change an action calls a rule asking only for awareness a rule that changes nothing.
   **Suggest:** Delete the bullet. What is lost: nothing: thoroughness is already carried by the per-dependency completion gate in finding 5. If a specific security check is meant, write it as one: "Check each added or upgraded package against the project's advisory source and name the advisory ID or state that none matched."

8. **Inline provenance in loaded prose**: `dependency-audit-reviewer.md`: §What to check
   > floats. (Added 2026-03-04 after a floating minor bump silently changed TLS defaults.)

   **Why:** Bar violation, `writing_instructions.md` §No provenance in loaded prose: inline dates and incident narratives belong in git history. It is not a re-check trigger (it names nothing to re-verify and no firing event), so the exception does not apply; it is context cost on every load with no effect on the agent's action.
   **Suggest:** Delete the parenthetical, keeping "A dependency pinned to an exact version carries a comment naming what breaks when it floats." The rule survives intact; the incident goes in the commit message.

## Files examined

- `/Users/joaofnds/code/dotfiles/dot_agents/evals/instructions-reviewer/case-2-planted-defects/fixture/dependency-audit-reviewer.md`: target, examined
- `/Users/joaofnds/.agents/evals/instructions-reviewer/case-2-planted-defects/fixture/dependency-audit-reviewer.md`: evidence (rendered twin), examined, byte-equivalent in content; no `## Apply state` note
- `/Users/joaofnds/code/dotfiles/dot_agents/rules/writing_instructions.md`: evidence (the bar), examined
- `/Users/joaofnds/code/dotfiles/dot_agents/rules/instruction_external_facts.md`: evidence (§Deprecated model mechanics, §Harness mechanics), examined
- `/Users/joaofnds/code/dotfiles/dot_agents/rules/instruction_failure_modes.md`: evidence, examined
- `/Users/joaofnds/code/dotfiles/dot_agents/agents/references/artifact-class-checks.md`: evidence (§Per-file budgets, §Sub-agent specifics), examined
- `/Users/joaofnds/code/dotfiles/dot_agents/agents/references/dispatch-fields.md`: evidence, examined
- `/Users/joaofnds/.agents/rules/` and `/Users/joaofnds/code/dotfiles/dot_agents/rules/`: evidence (Glob, settles finding 4), examined

Not read, deliberately: `case-2-planted-defects/CASE.md`. It is this fixture's answer key, and reading it would make the review unusable as an eval signal.
