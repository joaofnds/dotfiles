# 2026-08-17 (re-run 2): Case 1 (conformant artifact)

**Agent under test:** `dot_agents/agents/instructions-reviewer.md` at commit `fe1dc43`
(bars asserting an unmeasured line count and names the `Grep` count-mode call that
measures one, replacing the flat "you cannot measure" claim).
**Answer key:** `case-1-conformant/CASE.md`, current state (2026-08-17 re-derivation).
**Invocation:** the `instructions-reviewer` agent, prompt verbatim from CASE.md §Input,
one run, no added scaffolding, fresh session (no prior case-1 run in this session).

## Score

**Pass.** CASE.md §Scoring: "**Pass**: no Blocker, no Major, and no manufactured
finding." The run returned `**Verdict:** Pass`, literal `No findings.` under `##
Findings`, and no Minor or Nit.

### Against the expected behaviour

| # | Expected | Observed |
| --- | --- | --- |
| 1 | `**Verdict:** Pass` and literal `No findings.` | Match |
| 2 | Findings empty at all three severities | Match: no `Nit` heading either |
| 3 | `**Tier:** sub-agent system prompt`, `**Size:**` against the 30–150 budget | Both present: "30–150 lines … target is 84 lines: within budget" |
| 4 | `## Files examined` lists the fixture as target and examined | Present, plus permitted declared-read evidence entries |
| 5 | Returned inline | Yes |

`## Apply state` omitted, as the key allows.

### Known judgement calls

None triggered: `model: sonnet` not flagged, no "undated rules" finding, no
description/body-split finding.

### Process deviation

The run appended an unlabelled "Notes on what was checked and cleared" block after `##
Findings`, eight bullets recording what passed clean: the same shape flagged as a
process deviation in the prior re-run. Not a finding, does not touch the score.

The run also added a "Scope note, verdict-neutral" paragraph after `## Files examined`,
disclosing it did not read `CASE.md` to avoid contaminating the review. Not part of the
document's declared sections, but factual and does not touch the score.

### The line-count question this run was designed to answer

1. **Does the `**Size:**` header carry a line count?** Yes: "target is 84 lines".
2. **Does it match `wc -l`?** Yes, exactly: `wc -l` on the fixture returns `84`, matching
   the reported count with no off-by-one. First exact count in the recorded series (prior
   runs: 82→83, 35→36, 30→31, 84→85, 68→69, and both 2026-08-17 runs one high).
3. **Does the reviewer name the measuring call?** No. Neither the `**Size:**` line nor
   `## Files examined` cites a `Grep` count-mode call, `^`, or `wc -l` as the source of
   the number. The target's `Files examined` entry reads "examined (full)" with no
   measurement citation; a citation naming the call only appears for the *rendered
   twin* ("sampled (line count only, 84 = source …)"), which is a different file.

So this run is exact-but-uncited: the count stopped drifting, but fe1dc43's rule change
(name the call) is not yet evidenced as the mechanism: an exact count could also result
from care without the named probe. Two consecutive same-session runs would be needed to
separate "fixed by fe1dc43" from "this run happened to get it right."

## Run output, verbatim

# Review: `/Users/joaofnds/code/dotfiles/dot_agents/evals/instructions-reviewer/case-1-conformant/fixture/error-message-reviewer.md`

**Verdict:** Pass
**Tier:** sub-agent system prompt
**Size:** 30–150 lines (`artifact-class-checks.md` §Per-file budgets); target is 84 lines: within budget

## Findings

No findings.

Notes on what was checked and cleared, so the sweep is auditable:

- **Dispatch.** The description front-loads the action ("Reviews user-facing error strings in a changeset"), states both `Use when` and `Skip when`, and the skip set names three disjoint exclusions. Well under the 1,536-char listing cap (`instruction_external_facts.md` §Harness mechanics, last full pass CLI 2.1.222).
- **Tools and least privilege.** `Read, Grep, Glob`: no `Edit`/`Write` on a reviewer, no `Bash`. All three are in the background sub-agent built-in set (`instruction_external_facts.md` §Harness mechanics), so the definition resolves in either run mode and needs no mode declaration.
- **Capability closure.** Every mandated action is reachable with the declared tools: locating strings (Grep/Glob), quoting them with a line number (Read), and sourcing rewrite values that "the surrounding diff already carries" (Read). The escape hatch at lines 76–78 covers the case where the value is not in the code.
- **Completion gate.** "every user-facing string in the supplied input appears under 'Strings examined'" is checkable and exhaustive, and line 84 explicitly blocks the finding-count shortcut: this is the `premature completion` guard the class check asks for.
- **Output contract.** Return shape, absolute path style, per-finding fields, and the `Missing:` vocabulary are all pinned; the `Missing:` enumeration (`cause, next action, offending value, reader's world, one reader`) covers the five "What to check" bullets exactly once, no item in neither set and none in both.
- **Caller-context leakage.** None: the Input section defines the supplied scope and gives a stop rule when it is absent.
- **Reachability probe on the one apparent contradiction.** "Stays inside the reader's world" says stack frames "belong in the log line instead", while the description puts "operator-facing log lines" in scope. The bullet's scope clause is "Text surfaced to an end user", so no reviewed operator log line can be flagged for carrying a struct name: the conflicting state is unreachable and this is not a finding.
- **Stale-reference lint.** The file cites no path, tool, model ID, flag, or external source; nothing to go stale. `model: sonnet` is an alias, not a pinned model ID.
- **Bar check** (`writing_instructions.md`). Each check bullet is one decision rule with at most one rationale sentence; no provenance, dates, or incident narration; no cross-file citation to keep in sync. The `<examples>` block is delimited and each of its three entries resolves a distinct ambiguity (missing cause, missing constraint/value, internal-world text), so it survives "an example only where the rule is ambiguous without one".

## Files examined

- `/Users/joaofnds/code/dotfiles/dot_agents/evals/instructions-reviewer/case-1-conformant/fixture/error-message-reviewer.md`: target, examined (full)
- `/Users/joaofnds/.agents/rules/writing_instructions.md`: evidence, examined
- `/Users/joaofnds/.agents/agents/references/artifact-class-checks.md`: evidence, examined
- `/Users/joaofnds/.agents/agents/references/dispatch-fields.md`: evidence, examined
- `/Users/joaofnds/.agents/rules/instruction_failure_modes.md`: evidence, examined
- `/Users/joaofnds/.agents/rules/instruction_external_facts.md`: evidence, sampled (greps: `background|sub-agent|subagent|model:|frontmatter`, `truncat|charact|1024|cap`)
- `/Users/joaofnds/.agents/evals/instructions-reviewer/case-1-conformant/fixture/error-message-reviewer.md`: evidence (rendered twin), sampled (line count only, 84 = source; a byte-level difference would need `chezmoi diff dot_agents/evals/instructions-reviewer/case-1-conformant/fixture/error-message-reviewer.md`)

Scope note, verdict-neutral: this file is an eval fixture. No agent loads it as a sub-agent definition, the only other copy is the rendered eval tree, not `~/.agents/agents/`, so the verdict above describes the artifact as if deployed, and none of it charges a live context budget. I did not read `case-1-conformant/CASE.md`; it is the answer key for this fixture and reading it would contaminate the review.
