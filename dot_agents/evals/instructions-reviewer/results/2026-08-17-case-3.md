# 2026-08-17 — Case 3 (bare path, no diff)

**Agent under test:** `dot_agents/agents/instructions-reviewer.md` at commit
`9ababda748fadcd6194df73969246b0bf430f72c` (the reviewer rewrite landed in `1bfc61d`).
**Answer key:** `case-3-bare-path/CASE.md`, re-derived 2026-08-17.
**Invocation:** the `instructions-reviewer` agent, prompt verbatim from CASE.md §Input —
the bare path and nothing else. No artifact-type hint, no output instruction.

## Score

**Pass.** The standing-artifact mode is reachable from its barest legal call.

Case 3 defines no recall or precision — finding quality is explicitly not scored, since
the target is real corpus prose with no answer key. The findings are recorded below for a
human read.

### Against the expected behaviour

| # | Expected | Observed |
| --- | --- | --- |
| 1 | Proceeds; does not return the §Inputs request-for-input | Proceeded |
| 2 | Reviews `ownership.md`, not something guessed at | Reviewed `ownership.md` |
| 3 | Does not ask for a diff, requirements, or a changeset | Did not ask |
| 4 | Header: `Verdict`, `Tier: just-in-time rule`, `Size: 68 lines` against the just-in-time budget | All present, budget cited and its "length is fine *if* loaded on demand" clause quoted. **Size reported as 69; `wc -l` gives 68** |
| 5 | `Files examined:` lists `ownership.md` as target and examined | Present |
| 6 | Returned inline, unprompted | Yes |
| 7 | Reads `instruction_failure_modes.md`, `writing_instructions.md`, `artifact-class-checks.md` | Two of three. **`instruction_failure_modes.md` does not appear in the files-examined list** |
| 8 | Scope held — cited files and dependencies as evidence, never verdict-bearing targets | Held. One verdict, on the target only |

### Reads beyond the target and its cited files

CASE.md asks for these to be recorded. `ownership.md` cites `reporting_findings.md` and
`AGENTS.md` §Autonomy; both were read as ordinary evidence. Beyond those and the declared
dependencies, the run also opened `code-reviewer.md` (the only agent routing into
`ownership.md`), `answer-first.md` lines 80–91, `build/SKILL.md` lines 55–79, and grepped
`~/.claude/settings.json` for `outputStyle`. Each is cited as the evidence for a specific
finding — the co-load probe behind findings 1, 3, and 5 — and none received a verdict.
Recorded as widened evidence-gathering, not as a scope failure under the case's own
Fail rung ("gives a cited file its own verdict").

### Harness note

The output came back with a harness banner: the subagent's text matched an
instruction-shaped pattern (`settings-json`) and control tags were neutralized. The
trigger is finding 1, which quotes `"outputStyle": "answer-first"` from
`~/.claude/settings.json` as evidence. No directive-shaped text survived that needed
relaying. Recorded because it is the first time an eval run has tripped it, and a future
run quoting a settings file will trip it again.

## Answer-key notes for a human read

1. **The +1 line-count slip persists** — 69 reported against 68 actual, matching the
   README's record of the same slip in all three 2026-07-25 runs. Expected item 4 names
   the exact figure "68 lines", so a strict read fails item 4 on a cosmetic error. Worth
   deciding whether the key should name the budget rather than the count.
2. **Expected item 7 was only two-thirds met.** `instruction_failure_modes.md` is a
   declared read the run did not list. Either it was not read, or it was read and not
   recorded; the files-examined list is the only evidence available, so this cannot be
   settled from the output alone.

## Full findings, verbatim

---

# Review: /Users/joaofnds/code/dotfiles/dot_agents/rules/ownership.md

**Verdict:** Pass with revisions
**Tier:** just-in-time rule
**Size:** 69 lines / just-in-time budget — "length is fine *if* loaded on demand" (`artifact-class-checks.md` §Per-file budgets); loaded only via `AGENTS.md` §Task lifecycle, so no ceiling binds.

## Findings

### Blocker

None.

### Major

None.

### Minor

1. **Escalation-shape paragraph duplicates the always-loaded output style** — `dot_agents/rules/ownership.md` — §When you are blocked
   > Every question you hand up carries the same three parts, not only capability blocks

   Not an edit target — `dot_claude/output-styles/answer-first.md`, §(escalation, line 90): "Every escalation carries three things: one line of context, the one ask, and your"

   **Why:** Co-loaded near-duplicate. `~/.claude/settings.json` sets `"outputStyle": "answer-first"`, so that copy is in context in every session ownership.md ever loads into; the two already differ ("with its reason" in one, "not ready to ask" in the other), which is the drift a mirror produces when neither side is marked. The block-specific half is also already stated one paragraph up ("name every route you tried with the exact output that closed it, and ask for one thing").
   **Suggest:** Delete lines 18–20 (the whole paragraph). Nothing is lost: the always-on style carries the three-part shape for every escalation, and §When you are blocked keeps the routes-plus-one-ask requirement that is specific to a capability block.

2. **Closing paragraph restates the opening paragraph** — `dot_agents/rules/ownership.md` — §Priority (final paragraph)
   > The failure mode this prevents is silent tolerance, not bounded scope.

   **Why:** Instruction laundering within one path — the file's first paragraph already states both halves ("Own every problem you observe by making it visible and leaving an actionable next step. Ownership does not grant permission to expand the user's scope…"). A rule restated at the top and bottom of the same file signals the rule is unclear rather than making it stick, and the two wordings are now the pair a model reconciles by vibe.
   **Suggest:** Delete lines 66–68. The opening paragraph is the home; the added "silent tolerance" label names no action and imposes no condition.

3. **Reviewer carve-out stated on both paths it can reach** — `dot_agents/rules/ownership.md` — §Before Marking Done, step 3
   > ranks by severity instead and assigns none

   Not an edit target — `dot_agents/rules/reporting_findings.md`, §(preamble, line 7): "ranks by severity, assigns no disposition, and drops nothing — reading this as a"; `dot_agents/agents/code-reviewer.md`, §First, load the standard: "you rank by severity and drop nothing, and the caller assigns dispositions when it relays you"

   **Why:** Deliberate mirror with no mark on any of the three sides, and redundant on both reachable paths. Main-agent path: `AGENTS.md` §Task lifecycle loads `reporting_findings.md` alongside `ownership.md` whenever the report carries a finding — and a report with no finding never reaches this clause. Sub-agent path: `code-reviewer.md` is the only agent pointing here (grep for `ownership.md` across the repo returns it and `plan`/`build` skills only), and it states the carve-out itself while forbidding the `reporting_findings.md` read.
   **Suggest:** Cut the clause, leaving step 3 as: `3. List every failure or defect observed, with its evidence. When you are the agent reporting to the user, give each one a disposition (`reporting_findings.md` §Dispositions). A missing-thing claim carries its probe — §A missing thing is a claim.` Behavior is unchanged on both paths; the drift surface drops from three copies to two, each of which is the only copy on its path.

4. **Cross-file citations name no heading** — `dot_agents/rules/ownership.md` — §Before Marking Done step 3, §Priority (3 sites)
   > (`reporting_findings.md` decides which findings are Noted)

   **Why:** `writing_instructions.md` §Complete in place: "write cross-file citations as `file.md` §Heading". The reader sent to a 73-line file with no heading must scan it to find the disposition catalog, and a later section rename inside `reporting_findings.md` leaves the pointer silently unanchored.
   **Suggest:** Write all three as `reporting_findings.md` §Dispositions (verified: that heading exists at `dot_agents/rules/reporting_findings.md:11` and carries the Blocking/Decide/Noted definitions). If finding 3 is applied, only two sites remain. Cost: three words per site.

5. **Step 4's fix grant collides with a reviewer's read-only mandate; unreachable** — `dot_agents/rules/ownership.md` — §Before Marking Done, step 4
   > Fix defects that are within scope and low risk.

   Not an edit target — `dot_agents/agents/code-reviewer.md`, §(preamble): "You are **read-only**. Propose fixes as findings rather than applying them."

   **Why:** `code-reviewer.md` §First, load the standard sends the reviewer into this exact section for the done-bar, and step 3 carves the reviewer out of dispositions but leaves step 4's fix authorization uncarved — a complement stated for one step and left to inference for the next. Reachability probe: `code-reviewer.md` frontmatter declares `tools: Read, Grep, Glob`, so the harness makes the edit impossible; this is a maintenance note, downgraded to Minor on that probe rather than ranked as a live contradiction.
   **Suggest:** Extend step 3's existing carve-out phrasing to step 4 by scoping the grant: `4. When you are the agent reporting to the user, fix defects that are within scope and low risk.` Cost: eight words; it takes nothing from the surrounding rules and closes the inference for any future reviewer that gains a write tool.

## Files examined

- `/Users/joaofnds/code/dotfiles/dot_agents/rules/ownership.md` — target — examined
- `/Users/joaofnds/.agents/rules/ownership.md` — evidence (rendered twin) — examined — byte-for-byte identical to the source as read; no apply-state note
- `/Users/joaofnds/code/dotfiles/dot_agents/rules/writing_instructions.md` — evidence (the bar) — examined
- `/Users/joaofnds/code/dotfiles/dot_agents/rules/reporting_findings.md` — evidence — examined
- `/Users/joaofnds/code/dotfiles/dot_agents/AGENTS.md` — evidence (loading path) — examined
- `/Users/joaofnds/code/dotfiles/dot_agents/agents/code-reviewer.md` — evidence (citing agent, tool grant) — examined
- `/Users/joaofnds/code/dotfiles/dot_agents/agents/references/artifact-class-checks.md` — evidence (budgets) — examined
- `/Users/joaofnds/code/dotfiles/dot_claude/output-styles/answer-first.md` — evidence — sampled (lines 80–91; escalation section)
- `/Users/joaofnds/code/dotfiles/dot_agents/skills/build/SKILL.md` — evidence (commit-authorization citation, verified faithful) — sampled (lines 55–79)
- `/Users/joaofnds/.claude/settings.json` — evidence (`outputStyle` grep) — sampled
