# Iterate microtrial, 2026-09-15

Verdict: the data does not support changing ordinary triage or reflection from Opus high to medium. Any medium use remains an explicit opt-in experiment. The run also leaves preservation of the candidate instruction changes unverified.

## Frozen inputs

- Claude Code 2.1.270, configured as the `opus` alias. Provider envelopes identify `claude-opus-5` as the workflow model.
- Instruction criteria SHA-256 `b962e44b67eb2644b6c1eb9f6b17188539d211d311a4789b518b61210d10b8b2`.
- Baseline SHA-256 `e46d18e89fcde5d2d24ec8f0fa948678ef7acfa423541ea1df5aa7b8525ca11f`.
- Candidate SHA-256 `e7dfd36385f3965c9821b05cf2e90af3a703ab0ca5dd1aca4de041d5d4945c0f`.
- Eight one-shot calls with a $1 cap each. The scheduled ceiling was $8 against the $10 absolute trial-set budget.

The raw records are `/tmp/iterate-effort-trial-result` and `/tmp/iterate-instruction-trial-result`. They contain the exact system prompt and request, payload hashes, boundary receipts where an output exists, raw provider stdout and stderr, first outputs, deterministic checks, and provider usage.

## Results

The provider reported $2.90091 in list-price cost across all eight calls. Seven calls returned no model output because Opus 5 flagged the request with safeguard detail `[reasoning_extraction]`. The failures include both medium calls, the high reflection call, and all four baseline/candidate review-scope calls. Each failure record retains roughly $0.34 to $0.39 of cache-creation cost and its raw error.

Candidate-high triage was the sole returned output. It identified the contradictory evidence, retained the raw-record and coverage gaps, refused admission, selected the ready accepted card, made no board mutation, and asserted no completion. Its payload hashes and both boundary sentinels matched. It passed 14 of 15 frozen checks. The failed check expected `defer`; the output chose `reject`. The check remains failed because expected results were fixed before launch.

The high and medium arms cannot be compared for quality or cost because one produced a substantive output while the others ended at the provider safeguard. The baseline and candidate arms cannot establish instruction preservation because neither review-scope case produced an output. One substantive output cannot establish variation, production behavior, or general quality equivalence.

## Independent reading

Independent spec review examined the returned output beside its delivered request,
instruction payload, fixture, case manifest, and provider envelope. Its semantic
verdict is FAIL: the cost-reduction need is valid, but its quality evidence is
missing or contradicted. The triage criteria call for deferring that work until
its prerequisites are met. Rejecting it is the wrong disposition. The reviewer
confirmed the other decisions and evidence references. The seven calls without
outputs remain unverified.

## Harness changes after the run

The live records preserve the exact first-attempt inputs. After the calls finished, the checked-in review fixtures changed their example suffixes from `.ts` to `.ts.fixture` so Bun cannot discover them as repository tests. Their case paths changed with the suffixes, while their expected decisions did not. Future runs also use deterministic boundary sentinels and name raw provider stdout `provider-stdout.txt`. These changes were not rerun and do not alter the recorded verdict.

Independent code review also led to complete case fingerprints, approved input
roots, private output creation, frozen input bytes, full output-schema validation,
structural JSON comparisons, and bounded process-group shutdown. Local fake-provider
tests cover those changes. They have not been rerun against a paid provider and do
not change the verdict of the original eight calls.
