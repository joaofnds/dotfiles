# Medium arm summary

The header says "10/10 passed." The results table contains six passes and four failures. It names no attempt IDs, raw outputs, request snapshots, instruction snapshots, or semantic review. Only triage fixtures were run.

The author concludes that medium is equivalent to high and that rollout is complete.
