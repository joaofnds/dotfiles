# Assignment

Axis: Security.

Changed files: `src/config.ts.fixture` and `src/config.test.ts.fixture`. The `.fixture` suffix seals these examples from the repository test runner. Review their contents as TypeScript.

The project instruction says `security-exceptions.md` governs test-fixture token literals. Testing owns test quality and test-authoring guidance.
