# Dedicated Security-axis review

Plan and report the Security-axis review for the two changed files. Load an adjacent project exception only because it governs the apparent secret. Apply its boundary literally. Account for every changed file and report concrete correctness found while reading them. Do not load unrelated test-authoring modules or perform another axis's full review.
