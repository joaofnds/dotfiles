# Assignment

Axis: Style.

Changed files: `src/enabled.ts.fixture` and `src/enabled.test.ts.fixture`. The `.fixture` suffix seals these examples from the repository test runner. Review their contents as TypeScript.

Required Style standards: the core and TypeScript coding-style rules. Testing owns test quality and test-authoring guidance. No project exception file exists.
